# 会话记录（2026-06-11 至 2026-06-14）

---

## 一、项目背景

Mini_myHDF5_v5 是一个复合材料超声检测智能评估系统，基于 FastAPI + React 构建。主要功能包括：
- .nde 文件上传与 HDF5 数据解析
- A-Scan/B-Scan 波形可视化与逐帧播放
- 基于随机森林/深度学习的缺陷自动分类（5 类：Cp/Db/Dl/OK/Po）
- AI 对话评估（集成 DeepSeek 云端大模型）
- 基于 HB 7224-2020 的验收判定规则引擎
- 争议项登记与管理
- 超声检测报告生成

---

## 二、修改记录

### 2.1 `backend/signal_analysis.py` — 补充验收判定专用特征
- 新增 5 个特征字段：`amp_attenuation`（幅值衰减百分比）、`consecutive_abnormal`（连续异常帧数）、`peak_shift`（峰值偏移）、`waveform_corr`（帧间相关系数）、`porosity_estimate`（孔隙率估算）
- 这些字段是验收规则引擎（summary.json）中规则表达式所需的

### 2.2 `backend/acceptance_checker.py` — 修正判定逻辑
- 修复 B 级违规被忽略的 bug：之前只有 level="A" 才判不合格，现在按 action 字段判断
- 新增 `or` 规则表达式的支持

### 2.3 `backend/main.py` — 对话式验收、争议项、委托单

**验收判定融入对话：**
- `chat_ask` 中检测用户是否询问验收标准（关键词：验收、超标、合格判定等）
- 自动注入标准列表到 system prompt
- 当用户问"依据 XX 标准是否超标"时，自动执行验收判定，结果注入对话上下文
- AI 用自然语言解释判定结果

**争议项 API：**
- `POST /dispute/submit`：登记争议项，可选上传证据 ZIP
- `GET /dispute/list`：列出所有争议项
- 每个争议项在 `backend/disputes/{dispute_id}/` 下存档，含 `metadata.json`、证据 ZIP、原始 .nde 副本
- 修复：争议项编号不再是 `|委托单位/地址` 这种乱码，加了特殊字符过滤

**委托单解析与报告生成：**
- `POST /dispatch/upload`：上传已填写的委托单 .doc 文件，用 antiword 解析字段
- `chat_report` 增加 `dispatch_data` 参数，委托单字段自动填充到报告模板
- antiword 编码问题修复：改用 shell 管道输出到临时文件再读

**System Prompt 改进：**
- 用户质疑模型判断时：坚持专业判断、不轻易认错、引导上传证据
- 检测报告：先要求委托单，上传后再生成

### 2.4 `backend/trainer.py` — 委托单解析 + 报告模板

**`parse_dispatch_doc()`：**
- 用 antiword 解析委托单 .doc，提取表格中的字段
- 字段映射：委托单位→$VALUE0、零件名称→Part_name、材料牌号→Material_type 等

**`generate_inspection_report()`：**
- 增加 `dispatch_data` 参数
- 替换映射优先使用委托单数据
- report_id 清理：只保留字母数字和 `-_`

### 2.5 `frontend/EvaluationPanel.jsx` — 多处改进

**验收判定移除：**
- 删除验收判定按钮、Modal、状态变量
- 验收全部改为对话方式

**AScan 波形按需加载：**
- 仅当问题涉及波形/信号/缺陷分析时才拉取 A-Scan 波形数据
- 避免问"你是谁""介绍数据库"时加载无用数据

**停止回复按钮：**
- 回复中显示 ⏹ 停止按钮（灰色 `StopOutlined` 图标，与上传按钮风格一致）
- 点击中断 fetch 请求，保留已收到的内容
- 使用 AbortController 实现

**争议项 UI：**
- 仅在用户质疑（关键词：不对、不是、应该等）+ AI 提及争议项时，才弹出争议登记卡片
- 支持选择 ZIP 证据 + 填写描述

**委托单上传 UI：**
- 检测到"报告"关键词 → 无委托单时显示上传引导卡片，有委托单时直接显示报告按钮

**拖拽上传：**
- 根 div 绑定 onDragOver/onDragLeave/onDrop
- 拖拽时显示蓝色虚线边框
- 仅接受 .nde/.h5/.hdf5

### 2.6 `frontend/AScanViewer.jsx` — 自动标注 + 快捷键修复

**自动标注功能（三步流程）：**
1. 操作人员选一帧好区 → 按数字键 `0` 标注为无缺陷
2. 点击「自动标注」按钮 → 后端计算各帧与参考帧的相似度
3. 人工补标剩余 pending 帧

**相似度公式：**
```
similarity = 0.5×相关系数 + 0.15×均值差异 + 0.2×能量比 + 0.15×标准差比
```
阈值 0.85：≥0.85 自动标 OK，<0.85 待人工补标

**Slider 标记：**
- 初始加载：灰色小圆点 `●`
- 参考帧：蓝色 ★
- 自动 OK：绿色 ●
- 待补标：橙色 ○
- 人工标注后：绿色 ●（无缺陷）或红色 ●（有缺陷）
- 每次人工标注后 pending 计数实时更新
- 底部始终显示操作引导文字

**快捷键修复：**
- 删除自定义左右键监听（与 antd Slider 原生键盘导航冲突）
- 仅保留数字键 0-9 标注的监听
- 左右键白名单判断：`document.activeElement?.closest('.ant-slider')` 时让 Slider 原生处理

**其他前端调整：**
- 自动标注按钮去掉图标（纯文字）
- 播放速度默认值从 100ms 改为 80ms
- `App.jsx` 中 `playSpeed` 初始值改为 `useState(80)`

### 2.7 验收标准文件（standards/）

**index.json** 更新：
- HB 7224-2020：验收标准、适用 CF/EP、CF/BMI、GF/EP、CF/TP
- GJB 1038.1A-2004：方法标准

**HB/summary.json** 重写为真实 HB 7224-2020 条款：
- 7 种缺陷类型（裂纹、分层、孔隙、夹杂、脱粘、气孔、富树脂）
- 三等级（A/B/C）判定
- CScan 尺寸依赖（Z=(X+Y)/2）
- 成群分散缺陷面积占比规则

**GJB/summary.json**：标注为方法标准，不含验收判据

### 2.8 后端自动标注 API

`POST /auto_label_frames` 端点：
- 输入：参考帧索引、相似度阈值
- 计算各帧与参考帧的 4 维组合相似度
- 自动保存 OK 帧到 .nde 文件

---

## 三、研究成果

### 3.1 数据集
- 1,902 个 .nde 文件，5 类缺陷（Cp/Db/Dl/OK/Po）
- 材料体系：CF/EP（80%）、GF/EP（17.9%）、GFAF/EP（2.1%）
- 结构类型：Plate（47.7%）、BondPP（48.7%）、Taper（3.6%）

### 3.2 模型性能
- RF 5 类准确率：**87.7%**（最佳模型 `rf_Cp_Db_Dl_OK_Po_0.877`）
- DL 4 类准确率：**86.5%**（`deep_Cp_Db_Dl_OK_0.865`，不稳定）
- DL 训练震荡：epoch 2-7 之间测试准确率在 41%-78% 波动

### 3.3 研究报告
- `研究报告.md`：精简版，约 8,000 字
- `研究报告2.md`：完整版，20,391 中文字符，含附录 A-G

---

## 四、待办/未完成

1. XGBoost 模型尚未集成
2. C-Scan 成像功能尚未实现
3. 多种标准（HB 7224 + GJB）同时对比的功能未做
4. 批量 .nde 处理未实现
5. 验收判定报告（PDF 格式）未实现
