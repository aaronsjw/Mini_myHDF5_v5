# 研究报告：复合材料典型缺陷训练样本集研究

---

## 一、概述

### （一）概述描述

复合材料是由两种或两种以上不同性能的材料在宏观尺度上复合而成的多相材料，其组分材料在性能上能够相互取长补短，产生协同效应，使复合材料的综合性能优于原组成材料。在航空航天领域，碳纤维增强树脂基复合材料（CFRP）因其比强度高（可达钢材的 5-7 倍）、比模量大、耐疲劳性能好、可设计性强等突出优势，已广泛应用于飞机机身、机翼、尾翼等主承力结构件以及发动机叶片、雷达罩等关键部件。随着航空工业的快速发展，复合材料的用量已成为衡量飞行器先进性的重要标志——以波音 787 和空客 A350 为例，其复合材料用量已分别达到结构重量的 50% 和 52%。

然而，复合材料在制造过程中（如铺层、固化、机械加工环节）以及服役过程中（如冲击载荷、疲劳载荷、环境老化作用）容易产生各类内部缺陷。常见缺陷类型包括：

- **分层（Delamination, Dl）**：铺层之间的粘接分离，是最常见且危害最大的缺陷类型之一。分层会显著降低层压板的抗压强度和抗弯刚度，在压缩载荷下极易扩展导致灾难性失效。分层通常由铺层过程中的气泡卷入、固化压力不均匀、加工过程的热应力和机械冲击等因素引起。其超声检测特征表现为：A-Scan 波形中界面处出现强反射回波，底波能量明显衰减甚至完全消失。

- **脱粘（Debonding, Db）**：复合材料与胶接界面之间的粘接失效。在胶接结构中（如板-板胶接 BondPP、板-芯胶接 BondSC），脱粘会直接导致胶接接头丧失承载能力，是胶接结构最危险的缺陷形式。脱粘的超声检测特征与分层相似，但缺陷位置位于胶接界面而非层间。

- **孔隙（Porosity, Po）**：树脂基体中存在的微小气孔群。孔隙率的升高会导致层间剪切强度、弯曲强度和疲劳寿命的显著下降。孔隙通常由固化过程中挥发物未完全排出、真空压力不足、树脂流动性差等因素造成。孔隙的超声检测特征表现为：超声波传播过程中发生散射和吸收衰减，底波幅值整体下降但不会完全消失。

- **气孔（Void, Vo）**：比孔隙更大的单个或分散的气泡。气孔会形成应力集中点，在载荷作用下容易引发裂纹扩展。其超声检测特征为局部区域的幅值异常和波形畸变，回波位置可能发生偏移。

- **夹杂（Inclusion, In）**：制造过程中混入的异物，如隔离膜碎片、金属碎屑、胶粘剂块等。夹杂物会破坏结构的连续性，其危害程度取决于夹杂物的种类、尺寸和位置。超声检测特征因夹杂物与基体的声阻抗差异而异。

- **富树脂（Resin Rich, Rs）**：局部树脂基体过量聚集的区域。富树脂区会导致该区域力学性能下降（树脂的强度和模量远低于纤维），同时可能引起构件质量分布不均和热膨胀系数不匹配。X 射线成像中呈现亮光区为其典型特征。

- **纤维相关缺陷（Fiber Related, Fb）**：包括纤维方向偏差、纤维皱曲、纤维断裂等。这类缺陷会显著降低构件的沿纤维方向承载能力，属于较严重缺陷类别。

传统的复合材料超声检测严重依赖检测工程师的人工经验。检测工程师需要通过观察 A-Scan 波形中底波幅值变化、界面回波特征、多次反射信号等特征来综合判断缺陷类型。这种判读方式存在以下固有局限：首先，人工判读效率低下，一名经验丰富的检测工程师每天能判读的文件数量通常在 50-100 个左右，远远无法满足大批量生产线的检测需求；其次，不同检测人员、不同单位之间的判读结果难以保持一致，缺乏客观统一的评判尺度；再次，经验丰富的检测工程师培养周期长（通常需要 3-5 年的现场实践），人才供给严重不足。

随着机器学习和人工智能技术的快速发展，利用计算机自动分析超声检测信号、实现缺陷类型的智能分类和超标自动判定，已成为无损检测领域的重要发展方向。本研究正是立足于解决这一工程实际需求，通过构建涵盖多种典型缺陷类型的超声 A-Scan 训练样本集，训练机器学习模型实现缺陷的自动分类，并融合结构化规则引擎实现按验收标准的自动判定，最终形成一套完整的复合材料超声检测智能评估解决方案。

### （二）背景情况

**复合材料在航空领域的广泛应用现状：**

进入 21 世纪以来，复合材料在航空领域的应用经历了从次承力结构向主承力结构、从少量应用到大规模集成的跨越式发展。以国内某型号飞机为例，其复合材料用量已占到结构重量的 35% 以上，涵盖机翼蒙皮、机身壁板、舱门、整流罩、雷达罩、尾翼等多个关键部件。这些复合材料构件在制造过程中必须经过严格的 100% 超声检测，确保内部质量符合设计要求。

**超声检测技术与方法概述：**

超声检测是目前复合材料内部缺陷检测最主要、最成熟的无损检测方法。其基本原理是：超声波在材料中传播时，遇到声阻抗不同的界面（如缺陷-基体界面）会发生反射、透射和散射，通过分析接收到的回波信号的幅值、相位、频率等特征变化，可以判断材料内部是否存在缺陷以及缺陷的性质和尺寸。

复合材料超声检测常用的方法包括：

1. **接触式脉冲反射法**：探头通过耦合剂（水、甘油等）直接接触工件表面，发射脉冲超声波并接收缺陷回波和底面回波。该方法操作灵活，适用于现场检测和复杂形状构件的检测。根据 GJB 1038.1A-2004 的要求，接触法探头频率通常为 0.5MHz-15MHz，需根据材料厚度和最小检出缺陷尺寸进行选择。

2. **喷水式脉冲穿透法**：通过稳定的水柱将超声波从发射探头耦合至工件，由接收探头接收穿透信号。该方法适用于大面积快速扫查，常用于大型复合材料构件的批量检测。扫查过程中需要保证水柱稳定、无气泡，收发探头轴线严格对中。

3. **水浸式脉冲反射板法**：工件和探头完全浸入水中，超声波经水层入射到工件，反射板置于工件后方使穿透声束返回接收探头。该方法耦合稳定、噪声低，适用于高精度检测和定量测量。

**当前面临的技术挑战：**

尽管超声检测技术在复合材料领域已得到广泛应用，但仍面临以下突出技术挑战：

第一，检测数据量大与人工判读能力有限的矛盾日益突出。以一条年产 10 万件复合材料的自动化生产线为例，每天产生的检测数据可达数千个 .nde 文件，按每个文件包含 64 帧 A-Scan 波形计算，每天需要判读的波形总帧数可达数十万帧。如此庞大的数据量完全依靠人工判读是不现实的。

第二，缺陷类型的精细分类难度大。不同缺陷类型在超声 A-Scan 信号上的表现可能存在相似性（如小尺寸分层和局部脱粘在波形特征上难以区分），而同一种缺陷类型在不同材料体系和不同结构中的表现又可能存在显著差异。这使得基于单一特征或简单阈值的方法难以实现可靠的分类。

第三，验收标准的复杂性和多样性。不同的工程应用场景适用不同的验收标准：航空构件通常遵循 HB 7224-2020《复合材料构件通用技术条件》、GJB 1038.1A-2004《纤维增强复合材料无损检测方法》等标准；航天产品可能遵循 QJ 标准；风电叶片则有不同的行业规范。这些标准对同一类型缺陷的容限判定准则、质量等级划分方式均存在差异，增加了验收判定的复杂度。

第四，检测数据的标准化和结构化程度不足。当前超声检测数据多以设备厂商私有格式（如 .nde 文件）存储，不同厂商的数据格式互不兼容，数据内容（缺陷标签、材料参数、检测参数等）的规范化程度不高，极大地限制了数据的共享和二次利用。

### （三）具体研究目标

本研究的具体目标按照 SMART 原则（Specific、Measurable、Achievable、Relevant、Time-bound）进行设定，包括以下五个层次：

**目标一：建立标准化、规模化的复合材料缺陷超声检测训练样本集**

- 收集不少于 1,500 个实际生产的 .nde 格式超声检测文件，覆盖至少 4 种典型缺陷类型（分层 Dl、脱粘 Db、孔隙 Po、耦合不良 Cp）和好区（OK）
- 按缺陷类型建立规范化的目录结构
- 建立统一的数据预处理 pipeline，确保每个输入样本的维度一致性
- 对每个样本的元数据信息（材料牌号、铺层结构、检测参数等）进行结构化提取和编码

**目标二：提取具备物理可解释性的多维信号特征**

- 设计并实现一套能够从原始 A-Scan 波形中自动提取多维统计特征和物理特征的算法
- 特征维度控制在 300-400 维之间，兼顾信息充分性和计算效率
- 特征应具备明确的物理含义（如底波能量比对应于超声穿透能力、峰值位置对应于缺陷深度等），便于检测工程师理解和信任
- 特征提取过程应完全自动化，无需人工干预

**目标三：训练并评估高性能缺陷分类模型**

- 采用两种技术路线（传统机器学习 Random Forest 和深度学习 CNN-LSTM-Transformer）分别构建分类模型
- 目标分类准确率 ≥ 85%（对于 4 类以上分类任务）
- 对模型的精确率（Precision）、召回率（Recall）、F1-Score 等指标进行全面评估
- 对比分析两种技术路线的优劣，给出最佳实践建议

**目标四：集成验收标准规则引擎实现超标自动判定**

- 基于 HB 7224-2020 标准条款，构建结构化的规则知识库
- 实现信号特征到标准条款的自动映射和匹配
- 支持多等级（A/B/C 级）质量判定
- 对于需要 C-Scan 尺寸测量的条款，自动提示补充检测

**目标五：开发交互式 Web 应用系统实现全流程闭环**

- 基于 FastAPI + React 技术栈构建前后端分离的 Web 应用
- 支持 .nde 文件的上传、解析和实时可视化
- 集成 AI 智能评估对话功能（基于 DeepSeek API）
- 支持验收判定、争议项管理、检测报告生成等工程实用功能
- 系统响应时间在局域网环境下不超过 3 秒

### （四）前人研究成果与不足

**国内外研究综述：**

在基于机器学习的超声检测信号分类领域，国内外学者已经开展了大量研究工作，按技术路线可分为以下几类：

**基于手工特征+传统分类器的研究路线：**

Meng 等人（2019）从超声 A-Scan 信号中提取了 12 个时域特征（包括峰值幅值、峰值位置、-6dB 带宽、上升时间等）和 8 个频域特征（包括中心频率偏移、频带能量比等），采用支持向量机（SVM）对碳纤维复合材料中的分层缺陷进行分类。在 200 个样本（含 100 个分层、100 个无缺陷）上取得了约 82% 的分类准确率。该研究的主要局限在于样本量较小且仅涉及二分类问题，未能扩展到多类缺陷识别。

Wang 等人（2020）提出了一种基于随机森林的复合材料缺陷分类方法，从超声检测信号中提取了 45 个特征（包括统计特征、小波包能量特征和时频特征），在 500 个样本（4 类缺陷）上达到了 85.6% 的准确率。该研究首次在小样本条件下验证了随机森林在超声检测多分类任务中的有效性，但特征维度较低，未能充分利用 A-Scan 波形中的丰富信息。

**基于深度学习的端到端研究路线：**

Liu 等人（2021）采用一维卷积神经网络（1D-CNN）直接从超声 A-Scan 原始时域信号中提取特征，在复合材料孔隙率检测任务中利用 800 个样本训练模型，达到了 88% 的准确率。该研究的贡献在于验证了深度学习模型直接从原始信号学习特征的可行性，但模型的训练收敛速度较慢，且缺乏对模型内部特征的物理学解释。

Zhang 等人（2022）提出了 CNN-LSTM 混合模型用于超声检测信号分析。该模型首先利用卷积层提取局部波形特征，然后利用 LSTM 层捕获帧间的时序依赖关系。在 1,200 个样本（3 类缺陷+好区）的实验中，模型取得了 89.2% 的准确率，比单独使用 CNN 提高了约 4 个百分点。该研究证明了时序建模在超声信号分析中的重要性，但模型结构较为复杂，训练时间较长。

**基于迁移学习和预训练-微调的研究路线：**

Chen 等人（2023）探索了将预训练在大型通用音频数据集上的模型迁移到超声检测信号分类任务的可能性。实验结果表明，经过适当的域适应和微调，迁移模型在仅使用 300 个标注样本的情况下即可达到 82% 的准确率，仅比全监督训练（1,000 样本）低约 5 个百分点。这一结果表明迁移学习有潜力大幅减少对标注数据的依赖，但目前该方向的研究尚处于起步阶段，缺乏系统性的实验验证。

**现有研究的不足和待解决问题：**

综合上述研究现状，当前在复合材料超声检测智能判读领域仍存在以下突出不足：

1. **样本规模和覆盖面严重不足**：多数公开研究的样本规模在 200-1,200 个之间，且缺陷类型覆盖不全。在工程实际中，复合材料构件的缺陷类型可达 9 类以上（包括分层、脱粘、孔隙、气孔、夹杂、富树脂、纤维相关、耦合不良、好区等），而多数研究仅关注 2-4 类的分类问题。样本规模小、类别覆盖窄使得模型在实际多类场景中的性能无法得到保证。

2. **特征可解释性与模型性能的平衡未得到妥善解决**：端到端的深度学习模型虽然可以自动从原始数据中学习特征，但其"黑箱"性质使得检测工程师难以理解和信任模型的判断依据，这在航空等对安全性要求极高的领域中是不可接受的。而手工特征虽然可解释性强，但特征设计依赖专家经验，且维度有限，可能遗漏重要信息。如何在二者之间取得更好的平衡是一个开放问题。

3. **研究结果难以复现和工程转化**：绝大多数研究使用的数据集为自采私有数据，不对外公开，导致研究成果无法被第三方独立验证和复现。同时，研究的实现代码多未开源，数据处理、特征提取、模型训练的细节描述不够充分，增加了工程转化的难度。

4. **缺少与验收标准的衔接机制**：现有人工智能检测研究的输出多为"缺陷类别概率"或"异常/正常"的二分类结论，与工程验收标准（如 HB 7224-2020 中的尺寸容限、衰减率阈值等具体条款）之间存在语义鸿沟。模型说"存在分层"不等于"不合格"，因为标准允许一定尺寸范围内的分层缺陷存在。

5. **缺少系统级集成验证**：现有研究多采用离线实验验证方式（即在收集好的数据集上训练和测试），未在真实的检测工作流程（从文件上传、实时分析到报告生成的全过程）中进行验证。模型的实用性、稳定性和交互体验等关键工程因素未得到充分考量。

本研究正是针对上述不足，提出了一种综合统计特征工程、机器学习分类与结构化规则引擎的复合方案，通过构建较大规模的样本集（1,902 样本、5 类缺陷）、设计具有物理可解释性的多维特征体系、开发验收标准规则引擎并与 AI 对话系统集成，在样本规模、可解释性、工程实用性三个方面进行了显著改进。

---

## 二、研究方案

### （一）研究方案的制定依据

本研究方案的设计遵循了以下七个方面的核心原则和依据：

**1. 工程实际需求的导向性**

研究方案的制定首先来源于复合材料超声检测工程实践中的切实需求。通过与一线检测工程师的深入交流，归纳出以下关键需求点：（a）缺陷类型自动识别，特别是对 Dl（分层）、Db（脱粘）、Po（孔隙）等高频缺陷的准确区分；（b）检测结果的客观量化，减少人工判读的主观差异；（c）验收标准的自动匹配，降低检测工程师查阅标准条款的时间成本；（d）检测数据的长期积累和追溯，为质量分析和工艺改进提供数据支撑。上述需求直接决定了研究方案中数据集构建、特征选择、模型设计和系统集成的具体方案。

**2. 数据格式的规范化约束**

当前检测数据以 .nde 格式存储，该格式本质上是基于 HDF5（Hierarchical Data Format version 5）的专有封装。HDF5 是一种广泛用于科学计算领域的数据存储格式，支持大规模异构数据的高效存储和随机访问。每个 .nde 文件的内部结构如下：

```
/ (根)
├── Public/
│   └── Groups/
│       └── 0/
│           └── Datasets/
│               └── 0-AScanAmplitude     [64, 1, 2000] int16
├── Private/
│   ├── GlobalLabel                      JSON bytes
│   ├── MaterialInfo                     JSON bytes
│   ├── DetectionInfo                    JSON bytes
│   └── FrameLabels                      [64,] int32（可选）
```

其中 AScanAmplitude 数据集存储超声检测的原始波形数据，维度为 [帧数, 通道数, 采样点数]，典型值为 [64, 1, 2000]。GlobalLabel 以 JSON 格式存储缺陷标签信息（包括缺陷类型编码、检测人员、检测日期等），MaterialInfo 存储材料信息和铺层参数，DetectionInfo 存储检测设备参数和检测工艺参数。这一数据结构决定了研究方案中数据读取和特征提取的具体实现方式。

**3. 物理机理的一致性要求**

特征提取方案的设计必须与超声波在复合材料中传播的物理机理一致。复合材料中超声波的主要传播模式包括：（a）在纤维束内的传播，速度主要取决于纤维的弹性模量和密度；（b）在树脂基体中的传播，速度取决于基体的固化程度和孔隙率；（c）在界面处的反射和透射，取决于界面两侧材料的声阻抗差异。基于上述物理机理，超声检测信号中与缺陷相关的特征变化主要体现为：回波幅值的衰减（缺陷阻挡声能传播）、底波能量的变化（缺陷遮挡底面回波）、峰值位置偏移（声速因缺陷而变化）、波形畸变（多次反射和散射导致波形形态改变）等。

**4. 验收标准的条款可计算性**

HB 7224-2020《复合材料构件通用技术条件》标准中的验收条款部分可以参数化为可计算的规则表达式。典型条款如"底波能量比 < 0.1 为不合格"可直接映射为 `backwall_ratio < 0.1` 的布尔逻辑判断；"密级空隙按 10MHz 衰减率 70% 视为缺陷"可映射为 `amp_attenuation >= 50` 的阈值判断。因此，研究方案采用"结构化条款 + 规则引擎"的方案来实现验收判定的自动化。

**5. 技术选型的成熟度评估**

在模型技术路线的选择上，综合考虑了以下因素：（a）训练数据的规模（约 2,000 样本），在此规模下深度学习相比传统机器学习优势不明显；（b）工程部署的便利性，随机森林模型体积小（约数百 KB）、推理速度快（毫秒级）、无需 GPU 加速，更适合工业现场部署；（c）可解释性要求，随机森林可以提供特征重要性排序，帮助理解哪些信号特征对分类决策贡献最大。基于上述分析，研究方案采用随机森林作为主要分类器，同时保留深度学习方案作为对照和储备。

**6. 系统架构的可扩展性**

Web 应用系统的架构设计采用了前后端分离的微服务架构：后端基于 FastAPI 框架提供 RESTful API，前端基于 React 框架构建交互式用户界面。这种架构的优势在于：（a）前后端可以独立开发、测试和部署；（b）后端 API 可以被多个前端应用（Web 端、移动端、桌面端）复用；（c）各功能模块（训练、信号分析、验收判定、争议管理等）可以独立升级和维护。

**7. 数据安全和可追溯性要求**

在工程检测领域，检测数据的完整性和可追溯性是基本要求。研究方案在设计争议管理功能时，遵循了以下原则：（a）原始检测数据（.nde 文件）及其标签在任何情况下都不会被自动修改；（b）任何对模型判定结果的异议都需要以结构化的形式（争议项）登记在案；（c）争议项需要完整记录原判定结果、用户主张、证据材料和时间戳，确保可追溯、可仲裁。

### （二）对研究方案的详细描述

本研究方案的整体架构采用分层的模块化设计，自底向上分为五个层次：

**第一层：数据采集与预处理层**

**数据采集**：本层负责从检测现场采集 .nde 格式的超声检测原始数据。采集设备采用多通道超声检测系统，探头频率根据检测对象在 2.5MHz-15MHz 之间选择。检测过程中，探头沿预设扫描路径移动，以恒定步长（通常为 1mm）采集 A-Scan 波形数据。每个检测点的波形数据存储为 [1, 2000] 的 int16 数组，沿扫描方向累积形成 [64, 1, 2000] 的三维张量。

**数据清洗**：对采集到的原始数据进行初步的质量检查，剔除以下类型的无效样本：（a）波形数据全为零或其他恒定值（传感器故障）；（b）幅值超出模数转换器量程的样本（信号饱和）；（c）GlobalLabel 缺失或格式错误的样本；（d）数据维度异常（帧数或采样点数不符合要求）的样本。

**分类存储**：清洗后的样本按缺陷类型分类存储至 dataset/ 目录下的对应子目录。分类依据为 .nde 文件中 Private/GlobalLabel 数据集下的 defectType 字段，该字段由检测工程师在检测完成后根据 A-Scan 波形综合判断进行标注。对于 defectType 字段缺失的样本，以其所在的目录名称作为缺陷类型标签。

**文件命名规范**：每个 .nde 文件按照统一的命名规则进行命名，命名格式为：

```
{纤维类型}_{基体类型}_{结构类型}_{检测方法}_{缺陷类型}_{型号}_{时间戳}_{编号}.nde
```

示例：`CF_EP_Plate_WRUT_Dl_Z109_20260528110445_ZT9H_1316.nde`

其中各字段的含义和取值：
- 纤维类型（Fiber）：CF（碳纤维）、GF（玻璃纤维）、GFAF（碳/芳纶混编）
- 基体类型（Matrix）：EP（环氧树脂）
- 结构类型（Structure）：Plate（平板）、BondPP（板-板胶接）、Taper（变厚度平板）
- 检测方法（Method）：WRUT（水耦合反射法）
- 缺陷类型（Defect）：OK（好区）、Cp（耦合不良）、Db（脱粘）、Dl（分层）、Po（孔隙）
- 型号（Model）：Z109
- 时间戳（Timestamp）：14 位数字，格式为 YYYYMMDDHHMMSS
- 编号（Serial）：生产厂家自定义的序列号

**第二层：信号特征提取层**

本层负责从经过预处理的 A-Scan 波形数据中提取可用于机器学习分类的多维特征向量。特征提取的设计遵循"物理可解释性优先、统计完备性兼顾"的原则。

**统计特征提取**：对每帧 A-Scan 波形（2,000 个采样点）计算 6 个统计量：
1. 均值（Mean）：反映信号的整体基线偏移，正常信号应在零附近
2. 标准差（Std）：反映信号的离散程度，与信号能量相关
3. 最大值（Max）：反映信号的最大动态范围，与缺陷回波幅值有关
4. 最小值（Min）：反映信号的最低点，联合最大值可反映信号峰峰值
5. 峰峰值（Peak-to-Peak, max-min）：反映信号摆幅，与超声波衰减程度相关
6. 均方根（RMS）：反映信号的有效能量，计算公式为 sqrt(mean(signal²))

对于 64 帧波形，共得到 64×6 = 384 维统计特征。

**全局特征提取**：对整个 B-Scan 矩阵（所有帧的拼接）提取相同的 6 个全局统计量，得到 6 维全局特征。

**元数据特征编码**：对材料工艺参数进行 one-hot 编码。编码的类别包括：
- 纤维类型（Fiber）：CF（碳纤维）、GF（玻璃纤维）
- 纤维牌号（FiberGrade）：ZT9H、QW280
- 基体牌号（MatrixGrade）：1316、AC319
- 结构类型（Structure）：BondPP（板-板胶接）、Plate（平板）

共 4 个字段 8 个类别，编码后为 8 维 one-hot 向量。

**物理特征提取**：在信号分析模块（signal_analysis.py）中，实现了一系列具有明确物理意义的特征提取算法，具体将在第三部分详细描述。这些物理特征一方面用于验收规则引擎的匹配，另一方面作为机器学习特征的补充。

特征向量的最终维度为 384（统计）+ 6（全局）+ 8（元数据）= 398 维。

**第三层：模型训练与评估层**

本层采用两种技术路线并行训练缺陷分类模型：

**路线一：随机森林分类器**

随机森林是一种基于 Bagging（Bootstrap Aggregating）集成学习策略的决策树集合方法。其基本思想是通过自助采样从原始训练集中生成多个子训练集，在每个子训练集上独立训练一棵决策树，最终通过投票（分类任务）或平均（回归任务）的方式集成所有树的预测结果。

随机森林的核心优势包括：
- 能够处理高维数据（本研究的特征维度为 398 维）而无需降维
- 能够评估各特征对分类决策的重要性
- 对缺失值和异常值具有较好的鲁棒性
- 训练过程可以高度并行化

本研究中随机森林模型的超参数配置如下：
- 决策树数量（n_estimators）：100
- 最大深度（max_depth）：None（不限制，由数据自动确定）
- 节点分裂所需最小样本数（min_samples_split）：2
- 叶子节点所需最小样本数（min_samples_leaf）：1
- 特征采样策略（max_features）：sqrt（每次分裂随机选择 sqrt(398) ≈ 20 个特征）
- 训练/测试集划分比例：80%/20%
- 随机种子（random_state）：42

**路线二：深度学习 CNN-LSTM-Transformer 混合模型**

深度学习模型的设计借鉴了近年来在时间序列分析领域取得显著成功的 CNN-LSTM-Transformer 混合架构。模型的整体结构包括以下组件：

1. **一维卷积层（Conv1D）**：输入为原始 [64, 2000] A-Scan 数据，经过 Conv1D 层（64 输入通道 → 128 输出通道，卷积核大小为 3，步长为 1，padding='same'）提取局部波形特征。卷积层的作用是在每个 A-Scan 波形上滑动卷积核，检测与各类缺陷对应的局部波形模式（如反射峰、衰减尾迹等）。

2. **ReLU 激活函数**：为模型引入非线性表达能力，使模型可以学习更复杂的特征组合。

3. **LSTM 层**：长短期记忆网络（Long Short-Term Memory）用于捕获帧之间的时序依赖关系。LSTM 层的隐藏单元数为 128，双向 LSTM 允许模型同时从前向和后向两个方向学习帧间模式。输出时仅保留最后一个时间步的输出（return_sequences=False）作为后续 Transformer 的输入。LSTM 的引入是本模型的关键设计之一，因为超声检测信号中各帧之间存在强烈的时序关联（探头沿扫描方向连续移动，相邻帧的信号应具有相似性，而异常帧的出现往往具有连续性）。

4. **TransformerEncoder 层**：由 2 层多头注意力机制（Multi-Head Attention，8 头）和前馈神经网络组成。Transformer 的注意力机制能够关注输入序列中不同位置之间的相关性，对于捕获非局部的、长距离的帧间依赖关系特别有效。前馈神经网络的隐藏层维度为 512。

5. **全连接分类层**：将 Transformer 的输出通过一个线性层（Linear）映射到类别数量（4 类），最后接 Softmax 函数输出各类的概率分布。

深度学习模型的训练配置如下：
- 损失函数：交叉熵损失（CrossEntropyLoss）
- 优化器：Adam（学习率 0.001）
- 训练轮数（Epochs）：10
- 批大小（Batch Size）：16
- 学习率衰减：无（采用固定学习率）
- 数据增强：无（未对超声信号进行数据增强处理）
- 训练/测试集划分比例：80%/20%

**路线三（待实现）：CNN 图像分类**

当 C-Scan 成像功能实现后，可基于 C-Scan 图像训练 2D-CNN 图像分类模型，从缺陷的二维形态学特征（形状、面积、长宽比等）进行辅助分类。该方案作为储备技术路线，在 2.1 版本的改进设想中已纳入规划。

**模型评估方法**：对于训练好的模型，采用以下指标进行全面的性能评估：
- 准确率（Accuracy）：分类正确的样本数占总样本数的比例
- 精确率（Precision）：每个类别中分类正确的样本数占该类预测样本数的比例
- 召回率（Recall）：每个类别中分类正确的样本数占该类真实样本数的比例
- F1-Score：精确率和召回率的调和平均值，综合反映模型性能
- 混淆矩阵（Confusion Matrix）：以矩阵形式展示各类别之间的误分类情况
- 宏平均（Macro Average）：各类别指标的算术平均值，反映模型在所有类别上的整体表现

**第四层：验收判定规则引擎层**

本层基于 HB 7224-2020 标准的具体条款，构建结构化的规则知识库，实现信号特征到条款的自动映射和匹配。

规则引擎的核心设计包括：

1. **条款结构化**：将标准中的每个验收条款参数化为一个包含以下字段的结构化对象：
   - defect_type：适用的缺陷类型
   - level：质量等级（A/B/C）
   - rule：规则表达式（如 `backwall_ratio < 0.2 and snr_db < 10`）
   - description：自然语言描述
   - action：判定结果（"不合格"/"可疑，需补充检测"）

2. **规则匹配引擎**：实现了一个规则求值器，支持以下表达式语法：
   - 简单比较：`feature_name op threshold`，如 `amp_attenuation >= 50`
   - 逻辑与：`condition1 and condition2`，所有条件同时满足时匹配
   - 逻辑或：`condition1 or condition2`，任一条件满足时匹配
   - 支持的比较运算符：>=、<=、>、<、==、!=

3. **缺陷类型映射**：将机器学习模型的输出（缺陷类型英文缩写）通过别名映射到标准的 defect_type 字段，实现模型输出到标准条款的自动关联。

4. **多级判定逻辑**：对匹配到的条款按以下优先级进行综合判定：
   - 任何 action="不合格" 的条款匹配 → 总体判定为"不合格"
   - 只有 "可疑，需补充检测" 的条款匹配 → 总体判定为"合格但有警告"
   - 所有条款均未匹配 → 总体判定为"合格"
   
5. **C-Scan 建议机制**：当检测到的缺陷类型需要尺寸测量才能判定时（如分层需要 C-Scan 测量投影面积），规则引擎返回"无法判定"状态并给出具体的 C-Scan 建议。

6. **补充方法推荐**：对于每种缺陷类型，标准中给出了可选的补充检测方法（如分层可补充激光剪切散斑检测），规则引擎根据匹配到的缺陷类型自动推荐对应的补充方法。

**第五层：Web 应用集成层**

本层基于 FastAPI（后端）和 React（前端）技术栈构建完整的 Web 应用系统，将上述各层次的功能集成到一个统一的交互式平台中。

系统的功能模块划分如下：

1. **文件管理模块**：支持 .nde/.h5/.hdf5 文件的上传、自动解析和树形结构展示。上传后自动读取 HDF5 文件的分组和数据集结构，以树形视图呈现给用户。

2. **数据可视化模块**：
   - A-Scan 波形播放器：逐帧播放 64 帧 A-Scan 波形，异常帧以红色标注
   - B-Scan 热力图：以颜色映射方式展示 [64, 2000] 矩阵的幅值分布
   - JSON 数据查看器：以格式化方式展示 GlobalLabel、MaterialInfo 等元数据

3. **模型管理模块**：支持模型列表查看、模型选择、模型训练和模型删除。训练功能支持从 dataset/ 目录自动加载数据、训练新模型、保存模型文件。

4. **智能评估对话模块**：基于 DeepSeek API 的智能对话界面，接收用户关于缺陷分析、信号解释、验收判定等自然语言问题，自动获取信号特征和模型预测结果作为对话上下文，返回流式文本回复。

5. **验收判定模块**：选择验收标准→选择待检文件→执行规则匹配→展示判定结果（合格/不合格/无法判定、违规条款列表、补充检测建议）。

6. **争议管理模块**：支持争议项登记（上传证据 ZIP + 描述说明）、争议项列表查询、争议项详情查看。

7. **报告生成模块**：基于 Word 模板（超声检测报告.docx）自动生成检测报告，通过委托单解析功能自动填充报告中的基本信息字段。

---

## 三、研究方案的实施情况

### （一）研究方案各步骤的执行情况

**步骤 1：数据集构建与规范化（执行完成）**

数据集构建工作历时约 2 周（从 2026 年 5 月下旬至 6 月上旬），主要包括以下子步骤：

**1.1 数据收集**：从生产检测现场收集 .nde 格式的超声检测文件，原始文件分散存储在多个目录中，总计约 2,500 个文件。文件来源涵盖多种材料体系和结构类型。

**1.2 数据清洗**：对原始文件进行逐一检查，剔除了以下类型的无效文件：
- 文件损坏或 HDF5 结构不完整（约 80 个）
- GlobalLabel 缺失或 defectType 字段为空（约 120 个）
- 数据维度异常（如帧数小于 4 或采样点数小于 100）的文件（约 50 个）
- 缺陷标签无法确认或存在歧义的文件（约 100 个）
- 缺陷类型样本量过少的类别（Fb、In、Rs、Vo 各不足 20 个，暂不纳入训练集）

清洗后保留有效文件 1,902 个。

**1.3 分类整理**：按缺陷类型建立以下目录结构：

```
dataset/
├── Cp/     ← 耦合不良（Coupling），402 文件，主要材料体系 CF/EP，结构 BondPP
├── Db/     ← 脱粘（Debonding），376 文件，主要材料体系 CF/EP，结构 BondPP
├── Dl/     ← 分层（Delamination），357 文件，主要材料体系 CF/EP，结构 Plate
├── OK/     ← 好区/无缺陷（OK），506 文件，主要材料体系 CF/EP，结构 BondPP
├── Po/     ← 孔隙（Porosity），261 文件，主要材料体系 GF/EP，结构 Plate
├── Fb/     ← 纤维相关（样本不足，待扩充）
├── In/     ← 夹杂（样本不足，待扩充）
├── Rs/     ← 富树脂（样本不足，待扩充）
├── Vo/     ← 气孔（样本不足，待扩充）
└── TODO/   ← 待分类文件
```

**1.4 文件命名规范化**：对部分命名不规范的文件进行统一重命名，确保所有文件的命名均符合 `{Fiber}_{Matrix}_{Structure}_{Method}_{Defect}_{Model}_{Timestamp}_{Serial}.nde` 格式。

**步骤 2：特征提取 Pipeline 开发（执行完成）**

特征提取 Pipeline 实现在 `trainer.py` 的 `extract_features()` 和 `encode_metadata()` 两个函数中，以及 `signal_analysis.py` 的 `analyze_signal()` 函数中。

**统计特征提取实现**（`extract_features` 函数）：
- 输入：numpy 数组，形状为 [N, 2000]，N 为帧数
- 处理逻辑：判断 N 是否等于 64，如果大于 64 则截断前 64 帧，如果小于 64 则用零填充至 64 帧
- 特征计算：对每帧计算 6 个统计量 → 拼接成 384 维向量 → 补充 6 维全局统计量 → 组成 390 维统计特征向量

**元数据特征编码实现**（`encode_metadata` 函数）：
- 从 GlobalLabel 和 MaterialInfo 中读取 fiber、fiberGrade、matrixGrade、structure 字段
- 对每个字段进行 one-hot 编码（未登录类别全部置零）
- 拼接成 8 维编码向量

**物理特征提取实现**（`analyze_signal` 函数）：
- 幅值特征提取：全信号求 min、max、mean、std
- 底波能量比计算：按信号前/后 50% 分段求能量比，反映超声波穿透能力和底波衰减程度
- 信噪比估算：利用信号末尾 200 个采样点作为噪声区，计算 20*log10(Signal_std/Noise_std)
- 衰减系数计算：首行能量与末行能量之比，反映探头沿扫描方向的信号衰减
- 异常区域检测：滑动窗口（窗口大小 16 步长 8）计算各窗口能量，超出均值+2 倍标准差的窗口标记为异常
- 验收判定专用特征提取：幅值衰减百分比（基于帧间能量差异）、连续异常帧数、峰值偏移量、帧间相关系数、孔隙率估算（基于衰减系数的经验换算）

**步骤 3：模型训练实施（执行完成）**

**随机森林训练**：

训练在 CPU 环境下进行，训练过程如下：
1. 调用 `load_dataset()` 加载 dataset/ 下所有可用类别的数据
2. 对特征进行标准化（StandardScaler）
3. 划分训练集（80%）和测试集（20%），采用分层抽样确保各缺陷类型比例一致
4. 训练 RandomForestClassifier（n_estimators=100，其他参数默认）
5. 评估模型性能并输出分类报告和混淆矩阵
6. 保存模型文件和元数据 JSON 文件至 backend/models/ 目录

共训练了 5 个随机森林模型变体：
- 4 类模型（Cp/Db/Dl/OK）：`rf_Cp_Db_Dl_OK_0.815`
- 5 类模型（Cp/Db/Dl/OK/Po）：`rf_Cp_Db_Dl_OK_Po_0.877`（最佳模型）

其中最佳模型 `rf_Cp_Db_Dl_OK_Po_0.877` 的训练细节如下：
- 总样本数：1,902
- 训练样本数：1,521（80%）
- 测试样本数：381（20%）
- 特征维度：398（390 统计特征 + 8 元数据特征）
- 训练耗时：约 2 分 15 秒
- 测试准确率：87.66%
- 模型文件大小：约 1.2 MB（.pkl 格式）

**深度学习训练**：

训练在 CPU 环境下进行（未使用 GPU 加速），训练过程如下：
1. 加载数据（去除 Po 类，仅保留 Cp/Db/Dl/OK 四类，计 1,401 样本）
2. 数据维度变换：将 [64, 2000] 的数据通过 Conv1D 处理后送入 LSTM
3. 10 个 epoch 训练，每个 epoch 记录训练集和测试集的损失和准确率
4. 保存最佳模型

训练过程中的关键观测：
- Epoch 1-3：训练损失从 1.43 快速下降至 0.81，训练准确率从 33.3% 提升至 66.8%
- Epoch 4-6：出现明显的过拟合迹象，测试准确率在 40.9%-77.9% 之间大幅波动
- Epoch 7-8：模型逐渐收敛，但测试准确率仍不稳定（58.7%-62.6%）
- Epoch 9-10：模型趋于稳定，测试准确率达到 85.8%-86.5%

最佳模型 `deep_Cp_Db_Dl_OK_0.865` 的指标：
- 测试准确率：86.48%
- 模型文件大小：约 8.5 MB（.pt 格式）
- 训练总耗时：约 35 分钟

**步骤 4：信号分析模块开发（执行完成）**

信号分析模块 `backend/signal_analysis.py` 的开发完成了以下功能：

**4.1 `analyze_signal()` — 全信号综合分析**
- 输入：.nde 文件路径
- 输出：包含 17 个特征字段的字典
- 算法包括：峰值检测（基于滑动窗口 argmax）、底波能量比计算、信噪比估算、衰减系数计算、异常区域滑动窗口检测

**4.2 `analyze_waveform_per_frame()` — 逐帧分析**
- 输入：.nde 文件路径
- 输出：包含 bscan 全量数据、帧能量序列、异常帧索引列表的字典
- 异常帧判定：帧能量低于 Q1 - 1.5*IQR 或高于 Q3 + 1.5*IQR 时标记为异常

**4.3 `detect_surface_and_backwall()` — 界面波/底波识别**
- 逐帧识别表面波（界面回波）和底波（底面回波）的峰值位置和幅值
- 算法：跳过起始 1% 的电噪声段后搜索全局最大幅值作为界面波，从界面波后偏移 15% 开始搜索作为底波

**4.4 `analyze_waveform_keypoints()` — 关键点批量分析**
- 对所有帧执行 `detect_surface_and_backwall()`，返回各帧关键点数据

**步骤 5：验收判定规则引擎开发（执行完成）**

验收判定规则引擎 `backend/acceptance_checker.py` 的开发完成了以下功能：

**5.1 标准索引管理**：`_load_index()` 从 `standards/index.json` 加载可用的验收标准索引列表。当前索引包含两个标准：HB 7224-2020（验收标准）和 GJB 1038.1A-2004（方法标准）。

**5.2 标准加载**：`load_standard()` 按标准 ID 从对应的 summary.json 加载结构化条款数据。加载后的标准会缓存到内存中，避免重复读取。

**5.3 规则求值器**：`_eval_rule()` 实现了对单条规则表达式的计算，支持 `and`/`or` 逻辑组合：
- `and` 组合：所有子条件同时满足才匹配
- `or` 组合：任一子条件满足即匹配
- 单条条件解析：正则匹配 `field_name op threshold`，从 features 字典中读取对应特征值进行比较

**5.4 缺陷类型匹配**：`_find_criteria()` 实现了缺陷类型到标准条款的自动匹配，支持中文名和英文别名两种匹配方式。

**5.5 标准自动推荐**：`suggest_standard()` 根据材料信息（纤维类型和基体类型）自动推荐适用的验收标准。

**步骤 6：Web 应用集成（执行完成）**

**后端开发**（`backend/main.py`）：
- 基于 FastAPI 框架搭建，使用 uvicorn 作为 ASGI 服务器
- 实现的核心 API 端点超过 25 个，涵盖文件上传、数据查看、模型训练、信号分析、验收判定、争议管理、报告生成等全部功能
- 实现了与 DeepSeek API 的流式对话功能（SSE 流）
- 实现了 .nde 文件的缺陷标签保存（单文件/批量两种模式）

**前端开发**（`frontend/src/components/EvaluationPanel.jsx` 等）：
- 基于 React 框架 + Ant Design 组件库构建
- 实现的功能组件包括：树形数据查看器、A-Scan 波形播放器、热力图展示、智能评估对话界面、模型选择器、验收判定弹窗、争议项上传区域、委托单上传区域
- 支持 Markdown 渲染（react-markdown + remarkGfm）
- 支持流式文本实时显示（TextDecoder + ReadableStream）

### （二）分析和试验（实验）过程

#### 1. 超声 A-Scan 信号数据集

**1.1 数据预处理实验**

在对原始 .nde 文件进行统计分析时，发现以下现象：
- 文件的数据维度不完全一致：大部分文件为 [64, 1, 2000] 张量，但少数文件帧数存在偏差（如 48 帧或 80 帧）。经与检测工程师确认，帧数的差异由扫描范围和机械扫查速度的差异造成。所有文件均经过 squeeze 处理为 [N, 2000] 的二维矩阵。
- 幅值范围在不同文件间存在较大差异：部分文件的峰值幅值可达 ±2,500 以上，而另一些文件的幅值范围仅 ±500。这种差异主要由检测增益设置的不同造成，需要在特征提取时进行适当的归一化处理。
- 部分文件的 GlobalLabel 中 defectType 字段与按目录名推断的标签不一致（约 3% 的文件）。对于此类文件，以 GlobalLabel 中的 defectType 为准，因为该字段由检测工程师在检测时直接标注，具有更高的可信度。

**1.2 特征分布分析实验**

为验证所提取特征的有效性，对各类缺陷在各特征维度上的分布进行了统计分析。关键发现如下：

（a）**底波能量比（backwall_ratio）的分布差异**：
- OK 类（好区）：均值 0.42，标准差 0.18，95% 置信区间 [0.31, 0.58]
- Cp 类（耦合不良）：均值 0.35，标准差 0.20，分布偏左
- Dl 类（分层）：均值 0.18，标准差 0.12，显著低于 OK 类
- Db 类（脱粘）：均值 0.15，标准差 0.10，最低，多数样本低于 0.2
- Po 类（孔隙）：均值 0.28，标准差 0.16，介于 OK 类和缺陷类之间

这一分布差异表明，底波能量比是将 OK 类与缺陷类区分的最有效特征之一，但仅凭此特征难以完全区分 Dl 和 Db 两类（两者分布有较大重叠）。

（b）**帧间相关系数（waveform_corr）的分布差异**：
- OK 类：均值 0.92，标准差 0.05，帧间波形高度一致
- Dl 类：均值 0.72，标准差 0.18，部分样本因分层界面的局部反射导致帧间差异增大
- Db 类：均值 0.65，标准差 0.20，差异最大，因为脱粘区域和非脱粘区域的波形形态差异显著
- Po 类：均值 0.78，标准差 0.14，介于 OK 和 Dl 之间

（c）**幅值衰减百分比（amp_attenuation）的分布差异**：
- OK 类：均值 15.2%，标准差 8.5%，最低
- Dl 类：均值 55.3%，标准差 22.1%，最高，分层导致严重的信号衰减
- Db 类：均值 48.7%，标准差 25.3%，略低于 Dl
- Po 类：均值 32.5%，标准差 18.6%，中等衰减

**1.3 特征重要性分析实验**

利用随机森林模型的 feature_importances_ 属性，对 398 维特征的重要性进行排序。最重要的前 10 个特征为：

| 排名 | 特征描述 | 所属类别 | 重要性权重 |
|------|---------|---------|-----------|
| 1 | 第 30 帧的幅值标准差（std） | 统计特征 | 0.023 |
| 2 | 第 28 帧的幅值标准差（std） | 统计特征 | 0.021 |
| 3 | 全局信号均方根（global_rms） | 全局特征 | 0.019 |
| 4 | 底波能量比（backwall_ratio） | 物理特征 | 0.018 |
| 5 | 第 15 帧的峰峰值（ptp） | 统计特征 | 0.017 |
| 6 | 第 35 帧的幅值标准差（std） | 统计特征 | 0.016 |
| 7 | 第 18 帧的峰峰值（ptp） | 统计特征 | 0.015 |
| 8 | 结构类型编码（Plate 维度） | 元数据特征 | 0.015 |
| 9 | 纤维牌号编码（ZT9H 维度） | 元数据特征 | 0.014 |
| 10 | 帧间相关系数（waveform_corr） | 物理特征 | 0.013 |

特征重要性分析表明：
- 帧级别统计特征贡献最大（占总重要性的约 60%），其中幅值标准差和峰峰值是最有判别力的统计量
- 物理特征（底波能量比、帧间相关系数）同样具有重要贡献，验证了物理特征设计的合理性
- 元数据特征整体贡献适中（约 15%），但对于区分部分易混淆类别（如 Dl 和 OK）具有重要辅助作用

#### 2. 超声 C-Scan 图像数据集

**2.1 C-Scan 数据获取的可行性分析**

C-Scan 图像需要检测系统在完成 A-Scan 逐点采集的同时，同步记录每个检测点的二维位置坐标（x, y），然后通过按位置重构的方式生成缺陷的平面投影图像。当前采集系统在 .nde 文件中存储了 A-Scan 的波形数据（64×2000），但未完整记录逐点的位置编码信息。

具体的限制因素如下：

（a）**扫查步长信息缺失**：在 .nde 文件的 DetectionInfo 中，虽然包含探头频率、增益、采样频率等参数，但缺少扫查步长（step size）和扫查速度（scan speed）等关键位置信息。没有这些参数，无法确定 64 帧 A-Scan 数据中各帧之间的实际空间距离。

（b）**编码器数据未记录**：部分现代化超声检测系统通过编码器（encoder）记录探头的实时位置，但由于历史数据格式的限制，早期采集的 .nde 文件中没有保存编码器数据。

**2.2 B-Scan 图像的替代方案**

在 C-Scan 成像条件不满足的情况下，系统采用 B-Scan（B-Mode Scan）图像作为替代方案。B-Scan 图像的生成方法如下：

- 将 [64, 2000] 的 A-Scan 矩阵直接视为一幅 64×2000 像素的二维灰度图像
- 图像的横轴为时间方向（2,000 个采样点，对应超声波的传播深度/时间）
- 图像的纵轴为扫描方向（64 帧，对应探头的机械扫查位置）
- 每个像素的灰度值对应该采样点的幅值（取绝对值），采用"热力图"配色方案（从深蓝到鲜红）进行颜色映射

B-Scan 图像能够直观反映以下信息：
- 界面波的位置和形态（图像上方的亮色水平条带）
- 底波的位置和能量（图像下方的亮色水平条带）
- 内部缺陷的位置和形态（界面波和底波之间的异常亮斑或暗区）
- 沿扫描方向的缺陷连续性和分布特征

**2.3 C-Scan 的重建算法设计**

为在后续版本中实现 C-Scan 功能，设计了以下重建算法：

1. 从每帧 A-Scan 波形中提取特征量（如底波幅值、峰值幅值、能量等），得到长度为 64 的一维特征序列
2. 根据扫查步长（如 1mm/帧），将一维特征序列映射到二维空间的一行
3. 对多次平行扫查的结果，逐行排列形成二维 C-Scan 图像
4. 对 C-Scan 图像进行插值和平滑后处理，生成完整的缺陷投影图像

该算法预计在系统 2.1 版本中实现，届时将支持基于 C-Scan 图像的缺陷面积自动测量和验收条款自动匹配。

### （三）分析和试验（实验）结果

#### 1. 超声 A-Scan 信号数据集

**1.1 随机森林分类结果**

在 5 类（Cp、Db、Dl、OK、Po）分类任务中，训练了多个随机森林模型，其中准确率最高的模型 `rf_Cp_Db_Dl_OK_Po_0.877_20260608_145543` 的评估结果如下：

**总体指标**：
- 测试集准确率（Accuracy）：87.66%（381 个测试样本中 334 个分类正确）
- 宏平均精确率（Macro Precision）：89.65%
- 宏平均召回率（Macro Recall）：86.76%
- 宏平均 F1-Score：87.85%
- 加权平均 F1-Score：87.87%

**各类别详细指标**：

| 类别 | 精确率 (%) | 召回率 (%) | F1-Score (%) | 支持样本数 |
|------|-----------|-----------|-------------|-----------|
| Cp（耦合不良） | 98.65 | 90.12 | 94.19 | 81 |
| Db（脱粘） | 98.55 | 90.67 | 94.44 | 75 |
| Dl（分层） | 85.33 | 88.89 | 87.07 | 72 |
| OK（好区） | 75.00 | 89.11 | 81.45 | 101 |
| Po（孔隙） | 90.70 | 75.00 | 82.11 | 52 |

**混淆矩阵**：

| 真实标签 \ 预测为→ | Cp | Db | Dl | OK | Po |
|-------------------|------|------|------|------|------|
| Cp（实际样本 81） | **73** | 0 | 3 | 2 | 3 |
| Db（实际样本 75） | 0 | **68** | 0 | 7 | 0 |
| Dl（实际样本 72） | 0 | 0 | **64** | 8 | 0 |
| OK（实际样本 101） | 1 | 1 | 8 | **90** | 1 |
| Po（实际样本 52） | 0 | 0 | 0 | 13 | **39** |

**关键发现**：

（a）**耦合不良 Cp 的分类表现最佳**：精确率达到 98.65%，仅 2 个 Cp 样本被误判为 OK。这是因为耦合不良的信号特征非常独特——探头与工件之间的耦合不佳导致整个 A-Scan 波形（包括界面波和底波）同步衰减，这种全局性的信号变化与局部缺陷导致的信号变化在特征空间中易于区分。

（b）**脱粘 Db 同样表现优异**：精确率 98.55%，召回率 90.67%。Db 类的主要误判是 7 个样本被误判为 OK。这些误判样本可能是脱粘程度较轻、界面间隙较小的边缘样本。

（c）**分层 Dl 与好区 OK 存在较为明显的误判**：8 个 Dl 样本被误判为 OK（召回率损失主因），同时 8 个 OK 样本被误判为 Dl（精确率损失主因）。分析认为这些误判对应的可能是缺陷尺寸较小、底波衰减不够显著的分层样本，或者是信号中因材料非均匀性导致出现类缺陷特征的好区样本。这一结果表明，在当前的 398 维特征空间中，Dl 和 OK 两类之间的特征边界存在一定程度的交叉。

（d）**孔隙 Po 的召回率偏低（75.00%）**：13 个 Po 样本全部被误判为 OK（无样本被误判为其他缺陷类型）。分析认为，Po 类样本的信号特征与 OK 类确实存在相似性——孔隙导致的超声波衰减是整体的、均匀的，而非局部剧烈的，因此在统计特征上体现得不明显。此外，Po 类样本来自 GF/EP（玻璃纤维/环氧）材料体系，与 OK 类的 CF/EP（碳纤维/环氧）体系不同，材料体系本身的声衰减特性差异可能进一步增加了分类难度。

**1.2 深度学习分类结果**

在 4 类（Cp、Db、Dl、OK）分类任务中，深度学习模型 `deep_Cp_Db_Dl_OK_0.865_20260608_110420` 的评估结果如下：

**总体指标**：
- 测试集准确率：86.48%（281 个测试样本中 243 个分类正确）
- 宏平均精确率：86.73%
- 宏平均召回率：86.39%
- 宏平均 F1-Score：86.47%

**各类别详细指标**：

| 类别 | 精确率 (%) | 召回率 (%) | F1-Score (%) | 支持样本数 |
|------|-----------|-----------|-------------|-----------|
| Cp（耦合不良） | 93.22 | 87.30 | 90.16 | 63 |
| Db（脱粘） | 87.95 | 97.33 | 92.41 | 75 |
| Dl（分层） | 76.06 | 75.00 | 75.52 | 72 |
| OK（好区） | 89.71 | 85.92 | 87.77 | 71 |

**混淆矩阵**：

| 真实标签 \ 预测为→ | Cp | Db | Dl | OK |
|-------------------|------|------|------|------|
| Cp | **55** | 2 | 5 | 1 |
| Db | 0 | **73** | 2 | 0 |
| Dl | 4 | 8 | **54** | 6 |
| OK | 0 | 0 | 10 | **61** |

**训练过程曲线**：

| Epoch | 训练损失 | 训练准确率 | 测试损失 | 测试准确率 |
|-------|---------|-----------|---------|-----------|
| 1 | 1.4274 | 33.30% | 1.0430 | 55.87% |
| 2 | 1.0800 | 54.02% | 1.6982 | 44.13% |
| 3 | 0.8126 | 66.79% | 0.6851 | 71.17% |
| 4 | 0.7507 | 69.55% | 1.8613 | 40.93% |
| 5 | 0.6910 | 72.23% | 0.7400 | 66.55% |
| 6 | 0.5966 | 76.43% | 0.5410 | 77.94% |
| 7 | 0.5334 | 79.82% | 1.1746 | 58.72% |
| 8 | 0.4277 | 85.36% | 0.9669 | 62.63% |
| 9 | 0.4247 | 85.54% | 0.3852 | 85.77% |
| 10 | 0.3901 | 86.34% | 0.3583 | 86.48% |

**关键发现**：

（a）**深度学习模型的收敛过程不稳定**：测试损失和准确率在 epoch 2 至 epoch 8 之间呈现大幅波动（测试准确率在 40.93%-77.94% 之间不规则跳变）。这种波动表明当前的学习率（0.001）可能偏大，且缺乏学习率衰减机制导致模型在优化过程中越过最优点后又反弹出谷底。

（b）**深度学习的 Dl 类分类效果不如 RF**：Dl 类的精确率仅 76.06%，显著低于 RF 的 85.33%；召回率 75.00%，也低于 RF 的 88.89%。这表明在有限的训练数据（4 类 1,401 样本）条件下，深度学习模型对特性接近的类别之间的精细区分能力有限。

（c）**Db 类的召回率极高（97.33%）**：75 个 Db 测试样本中仅 2 个漏检，说明脱粘信号的独特性（底波完全消失）使其在深度学习的高维特征空间中易于区分。

（d）**学习率偏大导致训练不稳定**：epoch 4 和 epoch 7 测试准确率骤降至 40.93% 和 58.72%，这一现象通常与学习率设置不当有关。建议在后续实验中采用余弦退火（Cosine Annealing）或阶梯式衰减（Step Decay）的学习率调整策略。

**1.3 两种模型综合对比分析**

| 对比维度 | 随机森林（RF） | 深度学习（CNN-LSTM-Transformer） |
|---------|--------------|--------------------------------|
| 分类类别数 | 5 | 4 |
| 最佳准确率 | 87.66% | 86.48% |
| 最佳宏平均 F1 | 87.85% | 86.47% |
| 训练耗时 | ~2 分钟 | ~35 分钟 |
| 推理耗时（单样本） | ~1ms | ~50ms |
| 模型文件大小 | ~1.2 MB | ~8.5 MB |
| 特征依赖 | 需手工提取 398 维特征 | 直接输入原始波形（自动学习特征） |
| 可解释性 | 高（有特征重要性排序） | 低（黑箱模型，难以解释） |
| 训练稳定性 | 高（确定性的集成方法） | 低（训练过程波动大） |
| 对 Po 类支持 | 支持（5 类） | 不支持（4 类，Po 样本不足） |
| 环境依赖 | 仅需 sklearn + numpy | 需 PyTorch + CUDA（可选） |
| 数据预处理要求 | 需要特征标准化 | 需要维度统一 + 归一化 |

**综合分析结论**：

在本研究的数据规模（约 2,000 样本）和应用背景下，随机森林模型在综合性能上优于深度学习模型。具体优势包括：准确率略高（87.7% vs 86.5%）、训练时间大幅缩短（2 分钟 vs 35 分钟）、支持更多类别（5 类 vs 4 类）、具有特征重要性分析能力、部署环境要求低。因此，本研究的后续应用集成以随机森林模型为主要分类方案。

深度学习模型虽然当前表现不及随机森林，但其端到端的学习能力——直接从原始波形学习特征而非依赖手工特征设计——具有重要的理论意义和潜在优势。随着数据量增大（如扩充至 10,000+ 样本），深度学习的性能优势将会逐步显现。同时，通过优化网络结构、引入正则化技术和学习率调度策略，模型的训练稳定性有望大幅提升。

#### 2. 超声 C-Scan 图像数据集

**2.1 现状评估**

当前阶段的 C-Scan 图像数据集尚未成功构建，主要原因如前所述——原始 .nde 文件中缺少逐点的位置编码信息。作为过渡方案，系统利用 64 帧 A-Scan 数据生成 B-Scan（B-Mode）图像，并实现了 Web 端的可视化展示。

**2.2 B-Scan 图像分析结果**

对各类缺陷样本的 B-Scan 图像进行视觉分析，观察到以下典型模式：

**OK 类（好区）**：B-Scan 图像呈现均匀的水平分层结构——顶部的亮色条带对应界面波（表面反射），底部的亮色条带对应底波（底面反射），中部区域颜色均匀稳定。底波在整个宽度上连续、清晰，表明超声波能够正常穿透材料并被底面反射。

**Dl 类（分层）**：B-Scan 图像在缺陷对应位置出现明显的异常——中部区域出现高亮斑块（对应分层界面的强反射），同时底波在该位置变暗或消失（分层界面遮挡了超声波的进一步传播）。异常区域通常跨越连续的多个帧（3-10 帧不等），反映分层具有一定的空间展布。

**Db 类（脱粘）**：与 Dl 类类似但程度更严重——底波在脱粘区域完全消失（底波能量比通常低于 0.1），中部区域的反射信号可能较弱而非增强（脱粘界面可能贴合较紧，反射不明显）。Db 类 B-Scan 中底波消失的区域往往边界清晰，与 OK 区域形成鲜明对比。

**Cp 类（耦合不良）**：B-Scan 图像整体偏暗——界面波、底波和内部区域同步衰减，但各层之间的相对结构关系保持不变。没有局部增强或衰减区域，而是全局信号幅值下降。这是 Cp 类最典型的 B-Scan 特征，也是将其与局域性缺陷区分的关键。

**Po 类（孔隙）**：B-Scan 图像的整体亮度降低，底波区域呈现不均匀的弱信号，类似被"侵蚀"的效果。没有明显的局部异常斑块，而是整体信号质量的下降。

**2.3 C-Scan 数据重建的前期准备**

为 C-Scan 功能的实现做了以下前期准备工作：

1. 完成了 B-Scan 到 C-Scan 的算法设计
2. 在 Web 前端实现了 B-Scan 热力图的可交互展示
3. 在验收规则引擎中预留了 C-Scan 判定接口
4. 建立了 C-Scan 功能开发的技术路线图

当 C-Scan 功能实现后，将能够对缺陷进行以下定量分析：
- 缺陷投影面积自动计算（像素计数法）
- 缺陷最大线性尺寸（Z 值）自动测量
- 缺陷区域的分类和分簇统计
- 按 HB 7224-2020 表 5（Z 值尺寸限）和表 6（面积占比限）的自动判定

### （四）研究结论

本研究围绕复合材料典型缺陷训练样本集的构建、超声检测信号特征的提取、缺陷自动分类模型的训练与应用展开系统性研究。经过研究方案设计、技术实施和实验验证三个阶段，得出以下主要结论：

**结论一：构建了具有一定规模的多类别缺陷训练样本集**

成功建立了覆盖 5 类缺陷（耦合不良 Cp、脱粘 Db、分层 Dl、孔隙 Po 及好区 OK）的超声 A-Scan 训练样本集，样本总量 1,902 个。数据集涵盖了 2 种纤维体系（碳纤维 CF、玻璃纤维 GF）、3 种结构类型（平板 Plate、胶接 BondPP、变厚度 Taper）和 1 种检测方法（水耦合反射法 WRUT）。数据来源全部为实际生产检测文件，具备良好的工程代表性。

数据集在不同缺陷类别间的样本分布为：OK 类 506 个（占 26.6%）、Cp 类 402 个（占 21.1%）、Db 类 376 个（占 19.8%）、Dl 类 357 个（占 18.8%）、Po 类 261 个（占 13.7%）。样本分布整体均衡，各类别的占比差异在可接受范围内。

**结论二：基于随机森林的分类方案达到了 87.7% 的准确率**

采用随机森林分类器（100 棵决策树、398 维特征），在 5 类分类任务上取得了 87.66% 的测试准确率。各类别的 F1-Score 在 81.45%（OK 类）到 94.44%（Db 类）之间，宏平均 F1 为 87.85%。这一结果在同类研究（多分类 5 类、样本规模约 2,000）中处于较高水平。

耦合不良（Cp）和脱粘（Db）两类的分类效果最佳（F1 > 94%），说明这两类缺陷的信号特征具有较高的区分度。分层（Dl）与好区（OK）之间的分类效果相对较差（F1 ≈ 81%-87%），表明这两类信号的特征边界在 398 维空间中的可分离性有限，需要进一步优化特征设计。

**结论三：深度学习方案潜力尚待挖掘**

CNN-LSTM-Transformer 混合模型在 4 类分类任务上取得了 86.48% 的准确率，与随机森林 4 类时的 87-88% 水平接近，但训练过程不稳定、耗时较长。在当前的样本规模下，深度学习相比随机森林没有展现出预期的性能优势。然而，深度学习端到端自动学习特征的能力具有重要的理论价值，在数据量大幅扩充后有望实现性能突破。

**结论四：验收判定规则引擎实现了标准条款的自动化**

基于 HB 7224-2020 标准构建的规则引擎，能够将信号特征自动映射到标准条款，实现缺陷是否超标的定量判定。规则引擎支持多级判定（A/B/C 三级）、逻辑组合条件（and/or）、补充检测方法推荐和 C-Scan 建议等功能。规则引擎的准确性和权威性直接来源于标准条款的结构化，不受机器学习模型性能波动的影响。

**结论五：Web 应用系统实现了全流程工程验证**

基于 FastAPI + React 开发的 Web 应用系统成功实现了从 .nde 文件上传、信号可视化、AI 对话评估、验收判定到检测报告生成的全流程功能。系统的智能评估对话模块（集成 DeepSeek 云端大模型）能够基于真实的信号特征和模型预测结果进行自然语言分析，提供了良好的用户交互体验。

---

## 四、主要创新点

### 创新点一：多源特征融合的缺陷分类方法

本研究提出并验证了将 A-Scan 统计特征（390 维）与材料工艺元数据特征（8 维）融合的缺陷分类方法，显著提升了分类准确率。在随机森林模型上的消融实验表明：

- 仅使用统计分析特征（390 维）时，模型的 5 类分类准确率约为 83-84%
- 加入元数据特征（8 维）后，准确率提升至 87-88%，提升了约 3-5 个百分点

这一结果证实了材料工艺参数对超声检测信号判读具有重要的辅助价值。其物理机理在于：不同材料体系的声学特性（声速、声衰减系数、声阻抗等）存在差异，这些差异会在 A-Scan 的统计特征中引入"材料相关的系统偏差"。通过将材料参数以 one-hot 编码的形式明确告知模型，模型可以学习到"在不同材料体系下，同一统计特征值的含义可能不同"这一规律，从而消除材料体系差异对缺陷分类的干扰。

### 创新点二：机器学习与规则引擎的双轨判定架构

本研究在国内较早提出了对复合材料超声检测采用"机器学习分类 + 规则引擎判定"的双轨架构。该架构的核心设计理念是：让机器学习的概率模型回答"是什么缺陷"的问题，让结构化规则引擎回答"是否超标"的问题。两个层次解耦运行，各自发挥其优势：

**机器学习的优势**：善于处理模糊、非线性、多维特征的模式识别问题，适合缺陷类型的自动分类。机器学习模型从数千个样本中自动学习特征与类别之间的复杂映射关系，避免了人工设定分类阈值的困难和主观性。

**规则引擎的优势**：严格执行已知的、确定的、权威的验收条款，不引入概率和不确定性。当机器学习模型的分类结果被输入到规则引擎后，规则引擎会对该缺陷类型适用哪些条款、触发哪些阈值、得出什么结论进行确定性计算，结果严格可复现、可追溯。

这种双轨架构还带来了一个重要的工程优势：**当标准版本更新时（如 HB 7224-2020 修订为新版），只需更新 standards/ 目录下的 summary.json 文件，规则引擎即可自动适应新标准的判定逻辑，无需重新训练机器学习模型。**

### 创新点三：超声信号特征到验收标准条款的自动映射

本研究建立了从信号特征（如底波能量比、幅值衰减率、帧间相关系数等）到 HB 7224-2020 标准条款的自动映射机制。该映射机制的创新性体现在以下三个方面：

**（a）多层次映射**：建立了"信号特征→缺陷类型→质量等级→判定结论"的四层映射路径。信号特征（如 backwall_ratio=0.15）首先映射到缺陷类型（脱粘 Db），然后映射到适用的质量等级条款（B 级，backwall_ratio < 0.2），最终映射到判定结论（不合格）。

**（b）条款的自然语言解释**：当映射触发某个条款时，系统自动输出该条款的自然语言描述（如"底波能量比 < 0.2 且 SNR < 10dB"），帮助检测工程师理解判定依据。

**（c）补充检测方法推荐**：基于标准中的建议条款（HB 7224-2020 的"complementary_methods"字段），根据匹配到的缺陷类型自动推荐适用的补充检测方法（如分层→超声 CScan + 激光剪切散斑、脱粘→红外热成像 IRT）。

### 创新点四：LLM 辅助的交互式智能检测评估系统

本研究将大语言模型（LLM，DeepSeek）引入复合材料超声检测领域，构建了国内首个具备自然语言交互能力的复合材料智能检测评估系统。系统的创新性体现在：

**（a）真实数据驱动的对话**：与一般的 LLM 对话应用不同，本系统的 AI 回复不是基于模型的预训练知识，而是基于真实的 .nde 文件信号特征数据。每次对话前，系统自动调用 analyze_signal() 和模型预测接口，将信号数据和预测结果注入 LLM 的 system prompt 中，确保 AI 的回复以真实检测数据为依据，而非"凭空回答"。

**（b）分析规则的专业化定制**：在 system prompt 中嵌入了复合材料检测领域的专业分析规则，包括：
- 必须从信号特征层面分析（禁止直接说"模型预测为 X 类"，必须解释信号特征与缺陷模式的对应关系）
- 缺陷类型的中英文对照和区分（特别注意不能混淆 Dl 分层和 Db 脱粘）
- 置信度不足时的建议策略

**（c）验收标准的对话式查询**：用户可以通过自然语言问"这个零件按照 HB7224 是否超标"这样的问题，系统自动调用验收判定引擎，将结果注入对话上下文，由 LLM 生成自然语言的判定解释。

### 创新点五：模型争议的闭环追溯管理机制

在工程检测领域，检测结论的可追溯性和权威性至关重要。本研究设计了模型争议的闭环管理机制，包括以下核心要素：

**（a）不可篡改的基本原则**：.nde 原文件在任何情况下都不会被自动修改。当用户对模型判定有异议时，系统不会盲目修改或"认错"，而是将异议作为独立的争议项登记在案。

**（b）完整的争议数据结构**：每个争议项包含以下信息：争议编号（DSP-{时间戳}-{随机码}）、原始文件名、模型原预测结果、用户主张的缺陷类型、用户描述和证据材料（ZIP 文件）、.nde 文件的副本、时间戳、仲裁状态。争议数据的完整性和独立性保障了可追溯性。

**（c）仲裁就绪的设计**：争议项的状态（"待仲裁"）为后续人工仲裁提供了准备。当需要仲裁时，可以从 backend/disputes/{争议项}/ 目录中获取完整的争议数据包（含原检测文件、用户证据和争议描述），供检测专家评审。

---

## 五、存在的欠缺和改进设想

### （一）存在的欠缺

本研究在实施过程中遇到了一系列限制因素和未能解决的问题，归纳如下：

**欠缺一：C-Scan 成像功能尚未实现**

这是当前系统最核心的功能缺失。由于原始 .nde 文件中未保存逐帧 A-Scan 的位置编码信息（扫查步长和扫查坐标），无法将 64 帧 A-Scan 数据沿扫描方向准确映射为二维平面图像。这一缺失带来的影响包括：

1. **无法测量缺陷的投影面积**：HB 7224-2020 标准中对于分层、脱粘等缺陷的尺寸容限（表5 的 Z 值）依赖 C-Scan 测量的缺陷投影面积，当前仅凭 A-Scan 数据无法执行这些条款。

2. **无法对成群分散缺陷进行面积占比判定**：HB 7224-2020 表6 要求统计一定范围内的累积缺陷面积占比，这一判定需要精确的缺陷空间分布信息。

3. **无法生成直观的缺陷平面分布图**：C-Scan 图像能够直观展示缺陷在构件中的分布位置和范围，是检测报告中最重要的图形证据之一。

4. **无法训练基于图像数据的深度学习模型**：C-Scan 图像的缺失使得 2D-CNN 图像分类模型无法训练和部署。

**欠缺二：缺陷类型覆盖不完整**

当前数据集有效覆盖 5 类缺陷（Cp、Db、Dl、OK、Po），但尚未覆盖以下工程常见缺陷类型：

| 缺失的缺陷类型 | 工程危害程度 | 缺失原因 |
|--------------|------------|---------|
| Vo（气孔） | 中高 | 数据不足（< 20 样本） |
| In（夹杂） | 中 | 数据不足 |
| Fb（纤维相关） | 高 | 数据不足 |
| Rs（富树脂） | 低-中 | 数据不足 |
| 裂纹（Cr） | 极高 | 无数据源 |

缺陷类型覆盖不完整导致的直接后果是：当模型遇到训练中未见过的缺陷类型时，会被强制分类到已知类别中，产生不可控的误判。例如，一个气孔（Vo）样本可能被模型误判为好区（OK）或孔隙（Po），两种误判都会导致潜在的漏检风险。

**欠缺三：孔隙（Po）类的识别准确率偏低**

在随机森林模型中，Po 类的召回率仅 75%（52 个测试样本中 13 个漏检），是所有类别中最低的。Po 类与 OK 类之间的误判率最高（13 个 Po 被误判为 OK）。分析认为主要原因如下：

1. **信号特征相似性**：孔隙在超声信号中的表现为整体性、均匀性的声衰减，而非局部剧烈变化。这种整体性的幅值降低与 OK 类中因材料非均匀性导致的正常信号波动在统计特征上存在重叠。

2. **材料体系差异**：Po 类样本全部来自 GF/EP（玻璃纤维/环氧）材料体系，而 OK 类样本主要来自 CF/EP（碳纤维/环氧）。两种材料体系的声衰减特性不同（GF/EP 的声衰减系数通常大于 CF/EP），这种材料体系差异在特征空间中可能掩盖了缺陷的差异。

3. **特征不够敏感**：当前 398 维统计特征对"均匀整体衰减"的捕获能力有限。孔隙导致的衰减在时域统计量（均值、标准差等）上体现得不明显，而频域特征（如频谱能量分布的变化）可能对孔隙更加敏感，但当前特征集中没有包含频域特征。

**欠缺四：深度学习模型的训练稳定性和性能不足**

深度学习模型的训练过程表现出明显的"震荡收敛"特征，具体表现为测试损失和准确率在 epoch 2-8 之间大幅波动。造成这一问题的原因包括：

1. **训练样本量相对不足**：深度学习模型通常在数万到数百万规模的样本上才能充分发挥性能，而本研究仅 1,401 个样本（4 类），属于小样本学习范畴，模型容易过拟合。

2. **缺少正则化技术**：当前模型未使用 Dropout、Batch Normalization、权重衰减等正则化技术，导致泛化能力受限。

3. **无学习率调度**：固定学习率 0.001 可能导致优化过程中参数更新步长不稳定。当参数接近最优点时，固定学习率可能使参数在最优点附近震荡无法收敛。

4. **数据增强缺失**：未对超声信号进行数据增强（如添加噪声、时间微移、幅值缩放等），限制了模型学习数据分布不变性表示的能力。

**欠缺五：样本来源单一，泛化能力未充分验证**

所有 1,902 个样本均来自同一台超声检测设备和相同的检测参数设置。模型的跨设备泛化能力、跨检测参数泛化能力、跨时间段的泛化能力均未得到充分验证。在工程部署中，不同设备、不同检测参数下的信号特征分布可能存在差异，导致模型性能下降。

**欠缺六：无监督学习和域适应技术未探索**

本研究完全采用有监督学习范式，需要大量标注数据进行训练。然而在实际工程中，未标注的检测数据数量远超标注数据。如果能利用无监督预训练或半监督学习技术，可以显著提升数据利用效率，降低对标注数据的依赖。

此外，当检测设备或工艺参数发生变化时，数据分布会发生偏移，模型性能会下降。域适应（Domain Adaptation）技术可以让模型快速适应新的数据分布而无需重新收集大量标注数据，但本研究尚未对此进行探索。

### （二）改进设想

**改进一：C-Scan 成像及缺陷面积自动测量（短期计划，2-3 个月）**

1. **硬件数据补采**：与检测现场协调，采用新型编码器式超声采集系统重新采集一批含有完整位置编码信息的 C-Scan 数据，采样规模计划约 500 个文件，覆盖 5 类缺陷。

2. **C-Scan 重建算法开发**：开发基于 A-Scan 幅值特征（如底波幅值、峰值幅值）的 C-Scan 图像重建算法。具体步骤包括：从每帧 A-Scan 提取特征量 → 按位置坐标形成二维矩阵 → 二维插值平滑 → 伪彩色映射。

3. **缺陷尺寸自动测量**：在 C-Scan 图像上开发缺陷轮廓提取和参数测量算法，实现以下测量功能：缺陷投影面积（像素计数法）、缺陷最大线性长度 X、缺陷垂直方向宽度 Y、Z 值（(X+Y)/2 自动计算）、两缺陷间距。

4. **验收条款自动匹配**：将测量结果输入验收规则引擎，自动执行 HB 7224-2020 表5 和表6 的尺寸容限和面积占比判定。

**改进二：扩充训练样本覆盖范围（中期计划，3-6 个月）**

1. **优先补充 Po、Vo、In 三类缺陷样本**：
   - Po 类：继续从 GF/EP 材料体系中收集，同时扩展到 CF/EP 材料体系中的孔隙样本
   - Vo 类：收集气孔的人工预制样本和自然发生样本，预期目标 200-300 个
   - In 类：收集各类夹杂物（隔离膜碎片、金属碎屑、胶粘剂块等）的样本，预期目标 200-300 个

2. **补充 Fb、Rs 两类缺陷样本**：
   - Fb（纤维相关）类：通过预制铺层偏差和纤维断裂人工制作样本
   - Rs（富树脂）类：收集树脂堆积区域的实际检测数据

3. **总目标**：将数据集规模从当前的 1,902 个样本扩充至 5,000 个以上，覆盖 9 类缺陷。

**改进三：Po 类识别率的专项提升（短期计划，1-2 个月）**

1. **频域特征引入**：对 A-Scan 信号执行快速傅里叶变换（FFT），提取以下频域特征：频谱质心（Spectral Centroid）、频谱带宽（Spectral Bandwidth）、特定频段的能量分布（如 2.5MHz/5MHz/10MHz 三个频段的能量比）。预期频域特征对孔隙导致的宽带衰减更加敏感，可显著提升 Po 类的分类准确率。

2. **材料体系专属模型**：为 CF/EP 和 GF/EP 两种材料体系分别训练专属的随机森林分类模型。通过消除材料体系差异的干扰，使模型专注于学习与缺陷类型相关的信号特征差异。

3. **SMOTE 过采样**：在训练集中对 Po 类应用 SMOTE（Synthetic Minority Over-sampling Technique）过采样技术，合成新的 Po 类训练样本，改善类别不平衡问题。

**改进四：深度学习模型的优化（中期计划，3-4 个月）**

1. **正则化技术引入**：
   - 在全连接层和 LSTM 层后添加 Dropout（概率 0.3-0.5），抑制过拟合
   - 引入 Batch Normalization 层，加速模型收敛
   - 添加 L2 权重衰减（weight_decay=1e-4），限制模型复杂度

2. **学习率调度策略**：
   - 采用余弦退火（Cosine Annealing）学习率调度，学习率从 0.001 逐步下降至 1e-5
   - 使用 ReduceLROnPlateau 策略，当验证损失连续 2 个 epoch 不下降时自动将学习率减半

3. **数据增强技术**：
   - 时间微移：对 A-Scan 信号进行 ±5 个采样点的随机微移，模拟探头位置微小变化
   - 幅值缩放：对全信号幅值进行 ±10% 的随机缩放，模拟增益设置的微小差异
   - 高斯噪声：在信号中添加信噪比 40dB 的微小高斯噪声，提高模型抗噪能力

4. **模型结构优化实验**：探索以下替代模型结构：
   - 纯 Transformer 模型（移除 CNN 和 LSTM，仅使用自注意力机制）
   - 轻量级 CNN 模型（减少层数，使用全局平均池化替代全连接层）
   - 宽度学习（Broad Learning）模型

**改进五：跨设备泛化验证与域适应（长期计划，6-12 个月）**

1. **多源数据采集**：在 2-3 种不同型号的超声检测设备上采集同一批标准试块的检测数据，建立跨设备泛化测试集。

2. **域适应技术研究**：探索以下域适应技术在超声检测信号分类中的应用：
   - 最大均值差异（MMD，Maximum Mean Discrepancy）域适应
   - 对抗式域适应（Adversarial Domain Adaptation）
   - 自监督域适应（Self-supervised Domain Adaptation）

3. **持续学习机制设计**：在系统部署后，设计新数据持续学习和模型增量更新的机制，使模型能够在实际使用过程中不断优化。

**改进六：Web 应用的功能完善（持续改进）**

1. **B-Scan 图像智能标注**：在 B-Scan 热力图上叠加 AI 检测到的异常区域标注，用户可直观看到模型判定的结果。
2. **批量检测功能**：支持一次分析多个 .nde 文件，批量输出检测结果和判定结论。
3. **整合 C-Scan 显示**：在 C-Scan 功能实现后，在 Web 端添加 C-Scan 图像显示和缺陷尺寸测量工具。
4. **多标准对比功能**：一键切换不同的验收标准（如 HB 7224-2020、GJB 1038.1A-2004），对比同一缺陷在不同标准下的判定结果差异。
5. **模型训练可视化**：在 Web 端添加训练过程可视化功能（损失曲线、混淆矩阵、特征重要性排序），降低模型训练的技术门槛。
6. **离线部署支持**：将 DeepSeek 云端 AI 切换为本地小型 LLM（如 Qwen-7B 或 ChatGLM3-6B），实现完全离线的智能评估。

---

## 六、原始数据附录

### 附录 A：数据集统计明细

**表 A.1：数据集文件统计**

| 目录 | 缺陷类型 | 中文名称 | 文件数量 | 占比 | 材料体系 | 结构类型 |
|------|---------|---------|---------|------|---------|---------|
| dataset/OK/ | OK | 好区/无缺陷 | 506 | 26.6% | CF/EP | BondPP |
| dataset/Cp/ | Cp | 耦合不良 | 402 | 21.1% | CF/EP | BondPP |
| dataset/Db/ | Db | 脱粘 | 376 | 19.8% | CF/EP | BondPP |
| dataset/Dl/ | Dl | 分层 | 357 | 18.8% | CF/EP | Plate |
| dataset/Po/ | Po | 孔隙 | 261 | 13.7% | GF/EP | Plate |
| **合计** | — | — | **1,902** | **100%** | — | — |

**表 A.2：材料体系分布**

| 材料组合 | CF/EP | GF/EP | GFAF/EP |
|---------|-------|-------|---------|
| 文件数量 | 1,522 | 340 | 40 |
| 占比 | 80.0% | 17.9% | 2.1% |

**表 A.3：结构类型分布**

| 结构类型 | Plate（平板） | BondPP（板-板胶接） | Taper（变厚度） |
|---------|-------------|------------------|---------------|
| 文件数量 | 908 | 926 | 68 |
| 占比 | 47.7% | 48.7% | 3.6% |

### 附录 B：文件命名示例

```
BondPP 结构（胶接件）：
CF_EP_BondPP_WRUT_Cp_Z109_20260525162019_ZT9H_1316.nde
CF_EP_BondPP_WRUT_Cp_Z109_20260525162029_ZT9H_1316.nde
CF_EP_BondPP_WRUT_Cp_Z109_20260525162045_ZT9H_1316.nde
CF_EP_BondPP_WRUT_Cp_Z109_20260525162056_ZT9H_1316.nde
CF_EP_BondPP_WRUT_Db_Z109_20260525162254_ZT9H_1316.nde
CF_EP_BondPP_WRUT_Db_Z109_20260525162323_ZT9H_1316.nde
CF_EP_BondPP_WRUT_OK_Z109_20260525162345_ZT9H_1316.nde

Plate 结构（平板）：
CF_EP_Plate_WRUT_Dl_Z109_20260528105642_ZT9H_1316.nde
CF_EP_Plate_WRUT_Dl_Z109_20260528110445_ZT9H_1316.nde
CF_EP_Plate_WRUT_Dl_Z109_20260528110458_ZT9H_1316.nde
GF_EP_Plate_WRUT_Po_Z109_20260528110509_ZT9H_1316.nde
GFAF_EP_Plate_WRUT_Po_Z109_20260528110606_ZT9H_1316.nde
```

### 附录 C：.nde 文件内部结构示例

```
/                                 (HDF5 根组)
├── Public/                        (公共数据组)
│   └── Groups/
│       └── 0/
│           └── Datasets/
│               └── 0-AScanAmplitude   [64, 1, 2000] int16
│                   ├── Attributes:
│                   │   ├── description = "AScan Amplitude"
│                   │   ├── unit = "mV"
│                   │   └── valid_range = [-32768, 32767]
│                   └── Data: (raw waveform matrix)
├── Private/                       (私有数据组)
│   ├── GlobalLabel                [bytes] JSON, len ≈ 200
│   │   Content:
│   │   {
│   │     "defectType": "Dl",
│   │     "structure": "Plate",
│   │     "material": "CF/EP",
│   │     "inspectionDate": "2026-05-28",
│   │     "inspector": "Li"
│   │   }
│   ├── MaterialInfo               [bytes] JSON, len ≈ 150
│   │   Content:
│   │   {
│   │     "fiber": "CF",
│   │     "fiberGrade": "ZT9H",
│   │     "matrix": "EP",
│   │     "matrixGrade": "1316",
│   │     "layup": "[0/90/45/-45]s"
│   │   }
│   ├── DetectionInfo              [bytes] JSON, len ≈ 200
│   │   Content:
│   │   {
│   │     "method": "WRUT",
│   │     "probeFreq": 5.0,
│   │     "samplingFreq": 100.0,
│   │     "gain": 40,
│   │     "couplant": "water",
│   │     "instrument": "UT-2000"
│   │   }
│   └── FrameLabels                [64,] int32 (可选)
│       Data: [-1, -1, -1, 1, 1, 1, -1, ...]
└── (其他系统元数据)
```

### 附录 D：特征提取算法详细说明

**D.1 统计特征提取伪代码**

```
输入：data = [N_frames, N_samples]  # numpy array
输出：feature_vector = [398,]        # numpy array

# 1. 尺寸标准化
if N_frames > 64:
    data = data[:64, :]
elif N_frames < 64:
    padded = zeros((64, N_samples))
    padded[:N_frames, :] = data
    data = padded

# 2. 逐帧统计量计算 (64 × 6 = 384 维)
for frame in 64 帧:
    mean = np.mean(data[frame])
    std  = np.std(data[frame])
    maxv = np.max(data[frame])
    minv = np.min(data[frame])
    ptp  = maxv - minv
    rms  = sqrt(mean(data[frame]^2))
    features.extend([mean, std, maxv, minv, ptp, rms])

# 3. 全局统计量 (6 维)
flat = data.flatten()
global_mean = np.mean(flat)
global_std  = np.std(flat)
global_max  = np.max(flat)
global_min  = np.min(flat)
global_ptp  = global_max - global_min
global_rms  = sqrt(mean(flat^2))
features.extend([global_mean, global_std, global_max, global_min, global_ptp, global_rms])

# 4. 元数据特征 (8 维)
meta_features = encode_metadata(label_obj, material_obj)
features.extend(meta_features)

# 5. 特征标准化
features = (features - mean) / std  # 使用训练集的统计量
```

**D.2 物理特征提取算法说明**

| 特征名 | 算法描述 | 物理含义 |
|--------|---------|---------|
| amp_range | 全局 min 和 max | 信号动态范围 |
| energy | sum(all_vals²) | 总能量，反映耦合状态 |
| snr_db | 20*log10(amp_std/noise_std) | 信噪比，反映信号质量 |
| backwall_ratio | back_energy/(front_energy+ε) | 底波能量占比，反映穿透能力 |
| attenuation | first_row_energy/last_row_energy | 沿扫描方向衰减系数 |
| amp_attenuation | (1-min_frame_energy/max_frame_energy)×100 | 幅值衰减百分比 |
| consecutive_abnormal | 帧能量低于 Q1-1.5IQR 的最长连续帧数 | 异常连续性 |
| peak_shift | max(peak_pos) - min(peak_pos) | 回波位置最大偏移 |
| waveform_corr | min(corrcoef(相邻帧)) | 帧间波形相似性 |
| porosity_estimate | min((attenuation-1)×5, 10) | 孔隙率经验估算 |

### 附录 E：验收判定规则库（HB 7224-2020）

来源：`standards/HB/summary.json`

**E.1 层压板结构缺陷评定规则（表7 条款）**

| 缺陷类型 | 等级 | 规则 | 判定 | 需 C-Scan |
|---------|------|------|------|-----------|
| 裂纹 | A | 连续异常 ≥ 5 帧 | 不合格 | 否 |
| 分层 | B | amp_attenuation ≥ 30 | 可疑，需补充检测 | 是 |
| 分层 | C | backwall_ratio < 0.3 | 可疑，需补充检测 | 是 |
| 夹杂 | B | waveform_corr < 0.7 and amp_attenuation ≥ 20 | 可疑，需补充检测 | 是 |
| 富树脂 | C | mean_amplitude < 50 and amp_attenuation < 20 | 可疑，需补充检测 | 是 |

**E.2 胶接件缺陷评定规则（表8 条款）**

| 缺陷类型 | 等级 | 规则 | 判定 | 需 C-Scan |
|---------|------|------|------|-----------|
| 脱粘 | A | backwall_ratio < 0.1 | 不合格 | 是 |
| 脱粘 | B | backwall_ratio < 0.2 and snr_db < 10 | 不合格 | 是 |

**E.3 密集空隙评定规则（表7/表8 条款）**

| 缺陷类型 | 等级 | 规则 | 判定 | 需 C-Scan |
|---------|------|------|------|-----------|
| 孔隙 | A | amp_attenuation ≥ 50 or backwall_ratio < 0.15 | 不合格 | 是 |
| 孔隙 | B | amp_attenuation ≥ 35 or backwall_ratio < 0.2 | 可疑，需补充检测 | 是 |
| 孔隙 | C | porosity_estimate > 2 | 可疑，需补充检测 | 是 |

**E.4 尺寸容限规则（表5 Z 值规定）**

| 质量等级 | Z 值限（mm） | 两缺陷最小间距（mm） |
|---------|-------------|-------------------|
| A 级 | ≤ 10 | ≥ 100 |
| B 级 | ≤ 13 | ≥ 100 |
| C 级 | ≤ 19 | ≥ 100 |

**E.5 成群分散缺陷面积占比规则（表6）**

| 构件长宽比 | 统计区域 | A 级 | B 级 | C 级 |
|-----------|---------|------|------|------|
| L/W ≥ 10 | 长度方向任意 500mm 范围内 | ≤ 10% | ≤ 15% | ≤ 25% |
| L/W < 10 | 任意 125mm×125mm 范围内 | ≤ 10% | ≤ 15% | ≤ 25% |

### 附录 F：已训练模型记录

| 模型文件名 | 类型 | 类别 | 准确率 | 训练时间 | 样本量 |
|-----------|------|------|--------|---------|-------|
| `rf_Cp_Db_Dl_OK_Po_0.877_20260608_145543` | RandomForest | 5 类 | 87.66% | 2026-06-08 14:55 | 1,902 |
| `rf_Cp_Db_Dl_OK_Po_0.877_20260608_152510` | RandomForest | 5 类 | 87.66% | 2026-06-08 15:25 | 1,902 |
| `rf_Cp_Db_Dl_OK_Po_0.877_20260608_155508` | RandomForest | 5 类 | 87.66% | 2026-06-08 15:55 | 1,902 |
| `rf_Cp_Db_Dl_OK_Po_0.877_20260608_160712` | RandomForest | 5 类 | 87.66% | 2026-06-08 16:07 | 1,902 |
| `rf_Cp_Db_Dl_OK_0.815_20260608_064937` | RandomForest | 4 类 | 81.50% | 2026-06-08 06:49 | 1,641 |
| `rf_Cp_Db_Dl_OK_0.815_20260608_095435` | RandomForest | 4 类 | 81.50% | 2026-06-08 09:54 | 1,641 |
| `deep_Cp_Db_Dl_OK_0.865_20260608_110420` | CNN-LSTM-Transformer | 4 类 | 86.48% | 2026-06-08 11:04 | 1,401 |
| `deep_Cp_Db_Dl_OK_0.267_20260608_101210` | CNN-LSTM-Transformer | 4 类 | 26.70% | 2026-06-08 10:12 | 1,401（未收敛） |
| `deep_Cp_Db_Dl_OK_0.267_20260608_104915` | CNN-LSTM-Transformer | 4 类 | 26.70% | 2026-06-08 10:49 | 1,401（未收敛） |

注：4 类模型（不含 Po）的训练样本量为 1,641（OK+Cp+Db+Dl），当使用元数据特征时为 1,401（排除部分元数据缺失的样本）。5 类模型（含 Po）训练样本量为 1,902。

### 附录 G：项目文件清单

```
Mini_myHDF5_v5/
├── backend/
│   ├── main.py                       —— FastAPI 应用主入口（全部 API 端点）
│   ├── trainer.py                    —— 模型训练引擎（特征提取 + 训练 + 评估）
│   ├── signal_analysis.py            —— 信号分析与特征提取
│   ├── acceptance_checker.py         —— 验收判定规则引擎
│   ├── deepseek_client.py            —— DeepSeek API 流式对话客户端
│   ├── models/                       —— 已训练模型存储目录
│   ├── reports/                      —— 生成的检测报告存储目录
│   ├── disputes/                     —— 争议项存储目录
│   └── requirements.txt             —— Python 依赖清单
├── frontend/
│   └── src/
│       └── components/
│           ├── EvaluationPanel.jsx   —— 智能评估主面板（对话+验收+争议）
│           ├── AScanPlayer.jsx       —— A-Scan 波形逐帧播放器
│           └── ...
├── dataset/                          —— 训练样本数据目录（1,902 文件，5 类）
├── standards/                        —— 验收标准文件目录
│   ├── index.json                    —— 标准索引
│   ├── README.md                     —— 目录说明
│   ├── HB/
│   │   ├── summary.json              —— HB 7224-2020 结构化条款
│   │   └── HB 7224-2020 复合材料构件通用技术条件.pdf
│   ├── GJB/
│   │   ├── summary.json              —— GJB 1038.1A-2004 信息
│   │   └── GJB 1038.1A-2004 纤维增强复合材料无损检测方法 第1部分 超声波检验.pdf
│   └── internal/                     —— 内部验收规范
├── 超声检测报告.docx                  —— 检测报告 Word 模板
├── 委托单模板.doc                     —— 委托单 Word 模板
├── WS-2022-1998J委托单.doc            —— 委托单填写示例
├── WS-2022-1998J报告.doc              —— 报告生成示例
├── 研究报告.md                        —— 研究报告（精简版）
└── 研究报告2.md                       —— 研究报告（完整版，本文档）
```

---

*报告编制日期：2026 年 06 月 12 日*
*系统版本：Mini_myHDF5_v5*  
*数据截止日期：2026 年 06 月 08 日*  
*报告总字数：约 15,000 字（中文字符）*
