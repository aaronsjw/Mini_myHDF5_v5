# 研究报告：复合材料典型缺陷训练样本集研究

---

## 一、概述

### （一）概述描述

本报告围绕碳纤维/玻璃纤维增强树脂基复合材料超声检测（Ultrasonic Testing, UT）典型缺陷样本集的构建与智能识别展开研究。研究以实际生产中采集的 .nde 格式超声检测数据为基础，涵盖 5 类典型缺陷类型（耦合不良 Cp、脱粘 Db、分层 Dl、孔隙 Po 及无缺陷好区 OK），共计 1,902 个有效样本。研究内容包括样本集的规范化整理、多维信号特征的自动提取、基于机器学习和深度学习的缺陷分类模型的训练与评估，以及面向工程应用的验收判定规则引擎集成。

研究最终形成了一套完整的"数据采集→特征提取→模型训练→智能评估→验收判定"技术链条，并开发了配套的 Web 应用系统（Mini_myHDF5_v5），实现了从 .nde 文件上传、信号可视化分析、AI 对话评估到按标准验收判定的全流程闭环。

### （二）背景情况

复合材料因其比强度高、比模量大、可设计性强等优异性能，在航空航天、国防装备、风电叶片、体育器材等领域得到广泛应用。然而，复合材料在制造和使用过程中容易产生分层、脱粘、孔隙、夹杂等各类内部缺陷，这些缺陷会显著降低结构的承载能力和使用寿命。

超声检测是目前应用最广泛、技术最成熟的复合材料内部缺陷无损检测方法之一。然而，传统的超声检测依赖人工经验判读 A-Scan 波形，存在以下突出问题：

1. **判读效率低**：一名资深检测工程师每天可判读的文件数量有限，难以满足大批量检测需求；
2. **判读标准不一致**：不同检测人员对同一信号的判读结果可能存在差异，缺乏客观、统一的评判标准；
3. **验收规则复杂**：不同行业标准（如 HB 7224-2020、GJB 1038.1A-2004）对各类缺陷的容限要求不同，人工逐一对照条款效率低下；
4. **检测数据利用率低**：大量历史检测数据以 HDF5 专有格式存储，缺乏系统化的数据挖掘和知识积累机制。

基于上述背景，本研究立足于解决复合材料超声检测中的智能化判读问题，通过构建涵盖典型缺陷类型的训练样本集，训练机器学习模型实现缺陷类型的自动分类，并集成验收标准规则引擎，为检测工程师提供辅助决策支持。

### （三）具体研究目标

本研究的具体目标包括：

1. **建立标准化缺陷样本集**：对原始 .nde 格式的超声检测数据进行清洗、整理和分类，形成覆盖至少 4 种典型缺陷类型的训练样本集，样本总量不少于 1,500 个。

2. **提取有效的信号特征**：从超声 A-Scan 波形中提取具备物理可解释性的统计特征，包括幅值特征、能量特征、底波特征、信噪比等，形成维度合理（300-400 维）的特征向量。

3. **构建高性能分类模型**：分别采用传统机器学习（随机森林）和深度学习（CNN-LSTM-Transformer）两种技术路线建立缺陷分类模型，目标分类准确率不低于 85%。

4. **集成验收判定引擎**：将模型分类结果与行业验收标准（HB 7224-2020）结构化条款进行自动匹配，实现对缺陷是否超标的定量判定。

5. **开发交互式分析系统**：构建 Web 端可视化应用，支持 .nde 文件上传、A-Scan/B-Scan 波形显示、AI 对话评估、缺陷标签管理、检测报告生成等功能。

### （四）前人研究成果与不足

**国内外研究现状：**

近年来，机器学习在超声检测信号处理领域取得了显著进展。Meng 等人（2019）使用支持向量机（SVM）对碳纤维复合材料中的分层缺陷进行分类，在 200 个样本上取得了约 82% 的准确率。Liu 等人（2021）采用卷积神经网络（CNN）直接从超声 A-Scan 时域信号中提取特征，在孔隙率检测任务中准确率达到 88%。Zhang 等人（2022）将长短时记忆网络（LSTM）应用于超声信号序列分析，利用信号的时间相关性提升了脱粘缺陷的检测率。

在工程应用层面，国内外多家机构开发了超声检测智能判读系统。Olympus 公司的 OmniScan 系列仪器具备基本的信号分析和缺陷标记功能，但不具备自动缺陷分类能力。加拿大 TecScan 公司的 TECSSCAN 系统支持 C-Scan 成像和缺陷尺寸测量，但缺陷类型的自动判读仍需人工参与。

**现有研究的不足：**

1. **样本规模有限**：多数研究使用的样本集规模较小（200-500 个样本），且缺陷类型覆盖不全，难以支撑多分类模型的工程化应用。

2. **缺乏标准化的数据规范**：不同研究采用的数据格式和特征定义差异很大，不利于成果复现和工程转化。

3. **模型可解释性不足**：端到端的深度学习模型虽然准确率较高，但缺乏物理可解释性，检测工程师难以理解和信任"黑箱"输出。

4. **验收标准脱节**：机器学习模型的输出结论（如"存在分层"）无法直接映射到工程验收标准中的具体条款，实际应用时需要人工二次判断。

5. **系统集成度低**：现有研究多聚焦于单一的分类算法改进，缺乏从数据采集、模型推理到标准判定的完整工程闭环。

本研究针对上述不足，提出了一种融合统计特征分析、机器学习分类与结构化规则引擎的复合材料缺陷智能检测方案，在样本规模、特征可解释性和工程实用性方面进行了综合优化。

---

## 二、研究方案

### （一）研究方案的制定依据

本研究方案的制定遵循以下原则和依据：

1. **工程实用性优先**：选用实际生产现场采集的 .nde 格式超声检测数据，而非实验室条件下的理想数据，确保研究成果可直接服务于工程检测场景。

2. **多技术路线并行的对比验证**：同时部署传统机器学习（随机森林）和深度学习（CNN-LSTM-Transformer）两种技术路线，通过对比分析确定最适合本应用场景的技术方案。

3. **特征的可解释性**：优先提取具有明确物理意义的信号特征（如底波能量比、幅值衰减、峰值位置偏移等），而非完全依赖端到端的黑箱特征学习。

4. **结构化规则补充**：在机器学习分类基础上，叠加基于 HB 7224-2020 标准的结构化规则引擎，将"缺陷类型判断"与"是否超标判定"两个层次解耦，提升系统的可靠性和可追溯性。

5. **数据全生命周期管理**：从原始数据导入、特征提取、模型训练、在线推理到验收判定，形成完整的数据流闭环，支持争议项的登记和追溯。

### （二）对研究方案的详细描述

整体研究方案分为以下四个层次：

**第一层：数据层（Data Layer）**

数据集来自实际生产检测环节的 .nde 文件，文件采用 HDF5（Hierarchical Data Format version 5）格式存储。每个文件包含一个 [64, 1, 2000] 维的超声 A-Scan 张量数据——64 个扫描帧（frame），每帧包含 1 个通道、2,000 个离散采样点。此外，文件中还包含 Private/GlobalLabel（缺陷标签）、Private/MaterialInfo（材料信息）和 Private/DetectionInfo（检测参数）等元数据。

文件命名遵循统一的规范格式：
```
{纤维类型}_{基体类型}_{结构类型}_{检测方法}_{缺陷类型}_{型号}_{时间戳}_{编号}.nde
```
例如：`CF_EP_Plate_WRUT_Dl_Z109_20260528110445_ZT9H_1316.nde`

**第二层：特征层（Feature Layer）**

对原始 A-Scan 信号进行多维特征提取，包括：

（a）**统计特征**（390 维）：对 64 帧的每帧波形提取 6 种统计量（均值、标准差、最大值、最小值、峰峰值、均方根），构成 64×6=384 维特征；再对全局信号提取 6 维全局统计特征。

（b）**元数据特征**（8 维）：对纤维类型（CF/GF）、纤维牌号（ZT9H/QW280）、基体牌号（1316/AC319）、结构类型（BondPP/Plate）进行 one-hot 编码。

（c）**物理可解释特征**：除统计特征外，还提取了底波能量比（backwall_ratio）、信噪比（SNR）、幅值衰减百分比（amp_attenuation）、峰值位置偏移（peak_shift）、帧间波形相关系数（waveform_corr）等具有明确物理意义的特征，用于验收规则引擎的匹配。

**第三层：模型层（Model Layer）**

采用两种分类器进行对比实验：

（a）**随机森林（Random Forest）**：基于 Bagging 集成学习策略，构建 100 棵决策树，特征维度 398 维，覆盖 5 类缺陷。

（b）**深度 CNN-LSTM-Transformer 混合模型**：利用一维 CNN 提取局部波形特征，LSTM 捕获帧间时序依赖，Transformer 注意力机制增强全局特征表达，覆盖 4 类缺陷（不含孔隙 Po）。

**第四层：应用层（Application Layer）**

基于 FastAPI + React 构建 Web 应用系统，集成以下功能模块：

- .nde 文件上传与 HDF5 数据解析
- A-Scan 波形逐帧播放与 B-Scan 热力图显示
- AI 智能评估对话（集成 DeepSeek 云端大模型）
- 基于 HB 7224-2020 的验收判定规则引擎
- 争议项登记与管理
- 超声检测报告生成

---

## 三、研究方案的实施情况

### （一）研究方案各步骤的执行情况

**步骤 1：数据收集与整理**

从生产现场收集 .nde 格式超声检测文件，按缺陷类型分类存储至 dataset/ 目录下的对应子目录。共收集 5 个有效类别的 1,902 个文件，数据覆盖 3 种结构类型（Plate、BondPP、Taper）和 2 种纤维类型（CF、GF）。

| 缺陷类型 | 样本数量 | 占比 | 主要材料 | 主要结构 | 检测方法 |
|---------|---------|------|---------|---------|---------|
| OK（好区） | 506 | 26.6% | CF/EP | BondPP | WRUT |
| Cp（耦合不良） | 402 | 21.1% | CF/EP | BondPP | WRUT |
| Db（脱粘） | 376 | 19.8% | CF/EP | BondPP | WRUT |
| Dl（分层） | 357 | 18.8% | CF/EP | Plate | WRUT |
| Po（孔隙） | 261 | 13.7% | GF/EP | Plate | WRUT |
| **合计** | **1,902** | **100%** | — | — | — |

**步骤 2：特征提取 Pipeline 开发**

在 `trainer.py` 中实现了完整的特征提取流程：

- 自动遍历 dataset/ 下的所有子目录，递归加载 .nde 文件
- 从 HDF5 文件中读取 A-Scan 幅值张量（[64, 1, 2000] → squeeze → [64, 2000]）
- 逐帧计算 6 种统计量，拼接为 384 维特征向量
- 叠加 6 维全局特征和 8 维元数据特征，最终特征维度为 398 维
- 从 Private/GlobalLabel 中读取缺陷标签，建立特征-标签映射

**步骤 3：模型训练**

分别执行了随机森林和深度学习两类模型的训练：

**随机森林模型**——在 1,902 个样本（5 类）上训练：
- 数据集划分：80% 训练集（1,521 样本），20% 测试集（381 样本）
- 模型参数：n_estimators=100，其他参数默认
- 训练耗时：约 2 分钟
- 在扩展 5 类（含 Po）时，最佳模型 `rf_Cp_Db_Dl_OK_Po_0.877` 准确率 87.7%

**深度学习模型**——在 4 类（去除 Po 后 1,401 样本）上训练：
- 模型架构：CNN（1D 卷积 64→128 通道，核大小 3）+ LSTM（隐藏层 128）+ TransformerEncoder（2 层，8 头注意力）+ 全连接分类头
- 训练配置：10 个 epoch，batch size 16，学习率 0.001，Adam 优化器
- 最佳模型 `deep_Cp_Db_Dl_OK_0.865` 测试准确率 86.5%

**步骤 4：信号分析模块开发**

在 `signal_analysis.py` 中实现了一套完整的信号特征分析流程，从超声 A-Scan 数据中提取以下特征：

- **幅值特征**：幅值范围 [min, max]、平均幅值、幅值标准差、均方根能量
- **时域特征**：回波峰值位置（界面波/底波识别）、峰值偏移量
- **能量特征**：信号总能量、前段/后段能量比（底波能量比 backwall_ratio）
- **信噪比**：基于无信号区噪声估计的 SNR（dB）
- **衰减特征**：首行与末行能量比（衰减系数）、幅值衰减百分比
- **异常检测**：滑动窗口能量异常区域检测、逐帧异常标记
- **相关性特征**：帧间波形相关系数（检测波形畸变）
- **孔隙率估计**：基于整体衰减的孔隙率粗略估算

**步骤 5：验收判定规则引擎开发**

基于 HB 7224-2020《复合材料构件通用技术条件》标准，在 `acceptance_checker.py` 中实现了结构化规则引擎：

- 将标准条款参数化为可计算的规则表达式（如 `backwall_ratio < 0.1`、`amp_attenuation >= 50`）
- 支持 `and`/`or` 逻辑组合
- 按缺陷类型（分层、脱粘、孔隙、气孔、夹杂、裂纹）分条款匹配
- 三级质量等级判定（A/B/C 级）
- 需要 C-Scan 尺寸测量时自动提示补充检测

**步骤 6：Web 应用集成**

基于 FastAPI 后端和 React 前端构建了完整的 Web 应用，实现了 .nde 文件上传、树形数据查看器、A-Scan 波形播放器、B-Scan 热力图、AI 智能评估对话、模型训练与管理、验收判定、争议项登记、检测报告生成等功能模块。

### （二）分析和试验（实验）过程

#### 1. 超声 A-Scan 信号数据集

**数据采集规范**：所有 .nde 文件均使用水耦合反射式超声检测法（WRUT - Water-coupled Reflection Ultra-sonic Testing）采集，探头频率范围为 0.5MHz-15MHz，采样点数 2,000，扫描帧数 64。

**数据预处理**：对原始 [64, 1, 2000] 张量执行 squeeze 操作得到 [64, 2000] 的二维矩阵，按行（帧）提取特征。对帧数不足 64 的文件进行零填充，超过 64 帧的截断。

**特征分布分析**：不同缺陷类型的信号特征存在明显的统计差异。以关键特征为例：

- **底波能量比（backwall_ratio）**：OK 类样本通常在 0.3-0.6 范围；脱粘（Db）和分层（Dl）样本明显偏低（<0.2），因为缺陷阻挡了超声波的传播，导致底波信号减弱。
- **幅值衰减（amplitude attenuation）**：分层样本的幅值衰减百分比显著高于其他类型，衰减 50% 以上时判定为 A 级超标。
- **帧间波形相关系数（waveform_corr）**：OK 类样本的帧间相关性通常在 0.85 以上；夹杂和孔隙样本因局部信号畸变导致相关性降至 0.6 以下。

#### 2. 超声 C-Scan 图像数据集

当前系统尚未集成 C-Scan 成像功能。在现有硬件条件下，64 帧 A-Scan 数据沿扫描方向排列即可形成 B-Scan 图像（64×2000 像素矩阵），但受限于扫描步长的机械精度和缺少位置编码信息，当前尚不具备生成高分辨率 C-Scan 图像的条件。

**数据替代方案**：在验收判定规则引擎中，对于需要缺陷投影尺寸（Z 值）才能判定的条款，系统采用"AScan 初筛 + CScan 建议"的两步策略。即：规则引擎检测到需要尺寸信息时，返回"无法判定"并建议补充 C-Scan 检测。

### （三）分析和试验（实验）结果

#### 1. 超声 A-Scan 信号数据集

**随机森林分类结果（RF，5 类）：**

| 指标 | Cp | Db | Dl | OK | Po | 宏平均 |
|------|------|------|------|------|------|--------|
| 精确率（Precision） | 0.986 | 0.986 | 0.853 | 0.750 | 0.907 | 0.896 |
| 召回率（Recall） | 0.901 | 0.907 | 0.889 | 0.891 | 0.750 | 0.868 |
| F1-Score | 0.942 | 0.944 | 0.871 | 0.814 | 0.821 | 0.879 |
| 支持样本数 | 81 | 75 | 72 | 101 | 52 | 381 |

**混淆矩阵（RF，5 类）：**

| 真实\预测 | Cp | Db | Dl | OK | Po |
|-----------|------|------|------|------|------|
| Cp | **73** | 0 | 3 | 2 | 3 |
| Db | 0 | **68** | 0 | 7 | 0 |
| Dl | 0 | 0 | **64** | 8 | 0 |
| OK | 1 | 1 | 8 | **90** | 1 |
| Po | 0 | 0 | 0 | 13 | **39** |

**深度学习分类结果（CNN-LSTM-Transformer，4 类）：**

| 指标 | Cp | Db | Dl | OK | 宏平均 |
|------|------|------|------|------|--------|
| 精确率（Precision） | 0.932 | 0.880 | 0.761 | 0.897 | 0.867 |
| 召回率（Recall） | 0.873 | 0.973 | 0.750 | 0.859 | 0.864 |
| F1-Score | 0.902 | 0.924 | 0.755 | 0.878 | 0.865 |
| 支持样本数 | 63 | 75 | 72 | 71 | 281 |

**混淆矩阵（DL，4 类）：**

| 真实\预测 | Cp | Db | Dl | OK |
|-----------|------|------|------|------|
| Cp | **55** | 2 | 5 | 1 |
| Db | 0 | **73** | 2 | 0 |
| Dl | 4 | 8 | **54** | 6 |
| OK | 0 | 0 | 10 | **61** |

**训练过程记录（深度学习）：**

深度学习模型在 10 个 epoch 的训练过程中，训练损失从 1.43 降至 0.39，训练准确率从 33.3% 提升至 86.3%。测试准确率在 epoch 9-10 达到峰值 85.8%-86.5%，表明模型在第 6-7 个 epoch 后开始收敛，在第 9 个 epoch 后趋于稳定。

**模型对比分析：**

| 对比维度 | 随机森林（RF） | 深度学习（DL） |
|---------|--------------|--------------|
| 分类类别数 | 5（含 Po） | 4（不含 Po） |
| 最佳准确率 | 87.7% | 86.5% |
| 训练时间 | ~2 分钟 | ~30 分钟 |
| 特征依赖 | 需手动提取 398 维特征 | 从原始波形自动学习 |
| 可解释性 | 高（特征重要性可排序） | 低（黑箱模型） |
| 泛化能力 | 对 Po 类召回率偏低（75%） | 对 Dl 与 OK 易混淆 |

**关键发现：**

1. RF 模型的整体性能略优于 DL 模型（87.7% vs 86.5%），且训练时间显著更短，在本应用场景下 RF 更具工程实用性。

2. Dl（分层）和 OK（好区）是最容易混淆的两个类别，两者在混淆矩阵中错误率最高。物理上分析，部分 Dl 样本的底波衰减不够显著，信号特征接近 OK 类的边缘情况。

3. Po（孔隙）类在 RF 中召回率仅 75%，13 个 Po 样本被误判为 OK。原因是孔隙的超声信号特征（轻微衰减）与 OK 区域的正常波动范围存在重叠。

4. 元数据特征（纤维类型、基体牌号等）对分类准确率有显著贡献。在 RF 模型中，启用元数据特征后的准确率相比纯信号特征提升了约 3-5 个百分点。

#### 2. 超声 C-Scan 图像数据集

由于当前硬件条件的限制，C-Scan 图像数据集尚未成功建立。作为替代方案，系统基于 64 帧 A-Scan 数据生成的 B-Scan 图像（64×2000 像素）已在 Web 前端实现可视化显示，支持逐帧 A-Scan 波形播放和异常帧红色标注。

**B-Scan 实验结果：** B-Scan 图像能够直观反映缺陷沿扫描方向的分布。以 Dl（分层）样本为例，在 B-Scan 中可观察到明显的高幅值异常条带（对应分层界面的强反射），伴随底波能量区域的局部消失。OK 类样本的 B-Scan 呈现均匀的灰度分布，底波区域清晰连续。

### （四）研究结论

1. **样本集建设完成**：成功构建了覆盖 5 类缺陷、1,902 个样本的超声 A-Scan 训练样本集，数据覆盖 2 种纤维体系（CF、GF）、3 种结构类型（Plate、BondPP、Taper），具备较好的工程代表性。

2. **特征提取方案有效**：基于统计特征 + 元数据特征的 398 维特征向量，在随机森林分类器上达到了 87.7% 的 5 类分类准确率，验证了手工特征工程在超声检测信号分析中的有效性。

3. **随机森林优于深度学习**：在训练样本规模约 1,500-1,900 的条件下，随机森林的综合性能优于深度学习模型（87.7% vs 86.5%），且具有训练速度快、可解释性强的显著优势。

4. **验收判定规则可行**：基于 HB 7224-2020 标准的结构化规则引擎可实现缺陷类型到标准条款的自动匹配，AScan 初筛 + CScan 建议的两步策略兼顾了实用性与严谨性。

5. **系统集成验证**：开发的 Web 应用系统完成了"文件上传→信号分析→AI 评估→验收判定→报告生成"的全流程闭环验证，证明自动化检测评估方案在工程上是可行的。

---

## 四、主要创新点

1. **多源特征融合的缺陷分类方法**：提出将 A-Scan 统计特征（390 维）与材料元数据特征（8 维）融合的缺陷分类方案。经实验验证，元数据特征的加入使得 RF 分类准确率提升约 3-5 个百分点，证实了材料工艺参数对超声检测信号判读具有重要的辅助价值。

2. **机器学习与规则引擎的双轨判定架构**：将缺陷分类（机器学习负责"是什么缺陷"）与超标判定（规则引擎负责"是否超标"）分离，形成双轨判定流程。这种架构避免了单一模型承担所有判断任务的弊端，提升了系统的可靠性和可解释性。

3. **信号特征到标准条款的自动映射**：建立了从 A-Scan 信号特征（如底波能量比、幅值衰减率）到 HB 7224-2020 标准条款（如"5.3.2.4 缺陷评定"）的自动映射机制。检测工程师不仅看到判定结论，还能追溯到具体的违规条款。

4. **LLM 辅助的交互式智能评估**：将 DeepSeek 大语言模型引入复合材料检测领域，让 AI 能够基于真实的信号特征和模型输出进行自然语言分析。用户可以通过自然对话完成"分析信号"、"对比缺陷"、"验收判定"等操作，显著降低了系统使用门槛。

5. **模型争议的闭环管理**：设计了争议项登记和管理机制。当用户对模型判断有异议时，系统不盲目修改结论，而是引导用户提供证据材料并登记为争议项，供后续人工仲裁。这一设计保障了检测数据的可追溯性和权威性。

---

## 五、存在的欠缺和改进设想

### （一）存在的欠缺

1. **C-Scan 成像功能缺失**：受限于采集硬件的扫描位置精度信息缺失，当前系统无法生成 C-Scan 图像。因此，验收判定中依赖缺陷投影尺寸（Z 值）的条款（如 HB 7224-2020 表5 的尺寸容限）无法自动执行，需要人工补充 C-Scan 检测。

2. **缺陷类型覆盖不全**：当前样本集仅包含 5 类有效缺陷类型，夹杂（In）、气孔（Vo）、富树脂（Rs）等类型的样本数据严重不足（Fb、In、Rs、Vo 目录为空），导致模型无法覆盖全部工程常见缺陷类型。

3. **Po（孔隙）类识别率偏低**：孔隙类在 RF 模型中的召回率仅 75%，是 5 类中最低的。主要原因是孔隙信号特征与 OK 类的正常波动存在较大重叠，且 GF/EP 材料的声衰减特性与 CF/EP 不同，增加了分类难度。

4. **深度学习模型训练不稳定**：深度学习模型的训练过程中测试损失和准确率呈现明显的震荡现象（epoch 2 到 epoch 7 之间测试准确率在 41%-78% 之间大幅波动），表明模型可能存在过拟合或学习率设置不当的问题。

5. **样本来源单一**：所有样本均来自单一检测设备和同一检测环境，模型的泛化能力尚未在不同设备、不同检测参数下进行验证。

6. **无监督/半监督方法未探索**：当前方法完全依赖有监督学习，对未标注数据的利用能力不足。在实际生产中，大量检测数据缺乏精确的标签，若能利用半监督学习方法，可显著提升数据利用效率。

### （二）改进设想

1. **C-Scan 成像功能的实现**：通过读取检测设备的位置编码信息（编码器脉冲数），结合 64 帧 A-Scan 数据，沿扫描方向插值重建 C-Scan 图像，实现缺陷投影面积的自动测量。同时，可探索基于 B-Scan 图像进行语义分割的深度学习模型，直接从 B-Scan 图像中提取缺陷轮廓和尺寸。

2. **扩充训练样本覆盖范围**：有计划地补充夹杂（In）、气孔（Vo）、富树脂（Rs）、纤维相关（Fb）等缺陷类型的样本数据，使样本集覆盖全部 9 类常见缺陷类型（OK、Cp、Db、Dl、Po、Vo、In、Fb、Rs）。预计扩充后样本总量将达到 3,000 个以上。

3. **Po 类识别率的专项改进**：探索以下三种途径提升孔隙识别率：（a）引入频域特征（FFT 谱特征），因为孔隙在频域中的衰减特性比时域更明显；（b）对 GF/EP 材料单独训练专用的分类模型；（c）在特征空间中采用 SMOTE 过采样技术平衡类别分布。

4. **深度学习模型的优化**：（a）引入早停（Early Stopping）和学习率衰减机制，抑制训练震荡；（b）增加 Dropout 比例从 0.3 至 0.5，增强正则化效果；（c）探索更轻量级的模型结构（如纯 CNN + 全局平均池化），减少过拟合风险。

5. **跨设备泛化验证**：在至少 2-3 种不同型号的超声检测设备上采集测试数据，评估模型的跨设备泛化能力。必要时引入对抗域适应（Domain Adaptation）技术，消除不同设备间的数据分布差异。

6. **半监督/自监督学习探索**：利用大量未标注的 .nde 数据，采用对比学习（SimCLR、MoCo）等自监督预训练方法学习通用的超声信号表示，再在少量标注数据上进行微调，探索"预训练-微调"范式在本领域的适用性。

7. **多模态检测融合**：将超声 A-Scan 信号与红外热成像（IRT）、X 射线等其他无损检测方法的数据进行多模态融合，从多个物理维度综合判断缺陷类型和严重程度，提高检测置信度。

---

## 六、原始数据附录

### A. 数据集目录结构

```
Mini_myHDF5_v5/
├── dataset/
│   ├── Cp/           ← 耦合不良（402 文件）
│   │   ├── CF_EP_BondPP_WRUT_Cp_Z109_20260525162019_ZT9H_1316.nde
│   │   ├── CF_EP_BondPP_WRUT_Cp_Z109_20260525162029_ZT9H_1316.nde
│   │   └── …
│   ├── Db/           ← 脱粘（376 文件）
│   ├── Dl/           ← 分层（357 文件）
│   ├── OK/           ← 好区（506 文件）
│   ├── Po/           ← 孔隙（261 文件）
│   ├── Fb/           ← 纤维相关（待扩充）
│   ├── In/           ← 夹杂（待扩充）
│   ├── Rs/           ← 富树脂（待扩充）
│   └── Vo/           ← 气孔（待扩充）
├── backend/
│   ├── main.py                   ← FastAPI 主入口
│   ├── trainer.py                ← 模型训练引擎
│   ├── signal_analysis.py        ← 信号特征提取
│   ├── acceptance_checker.py     ← 验收判定规则引擎
│   ├── deepseek_client.py        ← DeepSeek API 客户端
│   ├── models/                   ← 已训练模型文件
│   └── disputes/                 ← 争议项记录
├── frontend/
│   └── src/
│       └── components/
│           ├── EvaluationPanel.jsx    ← 智能评估面板
│           ├── AScanPlayer.jsx       ← A-Scan 播放器
│           └── ...
├── standards/
│   ├── index.json
│   ├── HB/summary.json              ← HB 7224-2020 条款
│   └── GJB/summary.json             ← GJB 1038.1A-2004 信息
└── 超声检测报告.docx                ← 报告模板
```

### B. 文件命名规范说明

```
{ Fiber }_{ Matrix }_{ Structure }_{ Method }_{ Defect }_{ Model }_{ Timestamp }_{ Serial }.nde
```

| 字段 | 说明 | 示例值 |
|------|------|--------|
| Fiber | 纤维类型 | CF（碳纤维）、GF（玻璃纤维） |
| Matrix | 基体类型 | EP（环氧树脂） |
| Structure | 结构类型 | Plate（平板）、BondPP（板板胶接）、Taper（变厚度） |
| Method | 检测方法 | WRUT（水耦合反射法） |
| Defect | 缺陷类型 | OK、Cp、Db、Dl、Po |
| Model | 产品或型号 | Z109 |
| Timestamp | 时间戳 | 20260528110445（14 位数字） |
| Serial | 序列号 | ZT9H_1316 |

### C. 信号特征清单

`analyze_signal()` 函数输出的完整特征列表：

| 特征名 | 类型 | 说明 | 用于规则引擎 |
|--------|------|------|-----------|
| amp_range | [float, float] | 幅值最小值/最大值 | — |
| energy | float | 信号总能量 | — |
| snr_db | float | 信噪比（dB） | ✓ |
| mean_amplitude | float | 平均幅值 | ✓ |
| std_amplitude | float | 幅值标准差 | — |
| peak_location | float | 平均峰值位置（采样点） | — |
| backwall_ratio | float | 底波能量比 | ✓ |
| attenuation | float | 首末行衰减系数 | — |
| has_abnormal_zone | bool | 是否存在异常区域 | — |
| abnormal_zone_desc | str | 异常区域描述 | — |
| n_rows, n_cols | int | 信号矩阵尺寸 | — |
| **amp_attenuation** | float | 幅值衰减百分比（%） | ✓ |
| **consecutive_abnormal** | int | 最大连续异常帧数 | ✓ |
| **peak_shift** | float | 峰值位置最大偏移（采样点） | ✓ |
| **waveform_corr** | float | 帧间波形最小相关系数 | ✓ |
| **porosity_estimate** | float | 孔隙率估算值（%） | ✓ |

### D. 验收判定规则示例

HB 7224-2020 标准的结构化条款示例（来自 `standards/HB/summary.json`）：

| 缺陷类型 | 等级 | 规则表达式 | 判定结果 |
|---------|------|-----------|---------|
| 分层（Dl） | A | — | 超过对应等级 Z 值（A≤10/B≤13/C≤19mm）为不合格 |
| 脱粘（Db） | A | `backwall_ratio < 0.1` | 不合格 |
| 脱粘（Db） | B | `backwall_ratio < 0.2 and snr_db < 10` | 不合格 |
| 孔隙（Po） | A | `amp_attenuation >= 50 or backwall_ratio < 0.15` | 不合格 |
| 孔隙（Po） | C | `porosity_estimate > 2` | 可疑，需补充检测 |
| 夹杂（In） | B | `waveform_corr < 0.7 and amp_attenuation >= 20` | 可疑，需补充检测 |
| 气孔（Vo） | B | `peak_shift > 20 and waveform_corr < 0.8` | 可疑，需补充检测 |
| 裂纹（Cr） | A | `consecutive_abnormal >= 5` | 不合格 |

---

*报告编制日期：2026 年 06 月 12 日*
*系统版本：Mini_myHDF5_v5*
*数据截止日期：2026 年 06 月 08 日*
