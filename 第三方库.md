# v5 项目第三方库清单

## 一、后端（Python）

`backend/requirements.txt` 中的必装依赖：

| 库 | 用途 |
|---|---|
| fastapi | Web API 框架，所有接口端点 |
| uvicorn | ASGI 服务器，启动 `uvicorn main:app --reload` |
| pydantic | 请求/响应数据校验（BaseModel） |
| sse-starlette | SSE 流式响应（DeepSeek 对话流式输出） |
| h5py | HDF5 解析，读写 `.nde` 数据与元数据 |
| numpy | 数值计算（信号处理、特征提取、张量操作） |
| pandas | 数据处理（CSV 解析等） |
| scikit-learn | 随机森林 RandomForestClassifier、train_test_split、评估指标 |
| joblib | 模型序列化（RF 模型 `.pkl`） |
| python-multipart | 文件上传表单解析 |
| python-docx | Word 报告生成、`.docx` 委托单解析 |
| lxml | XML 处理（docx 内部 XML，报告占位符替换） |
| httpx | DeepSeek API 客户端（异步流式调用，见 `deepseek_client.py`） |
| python-dotenv | 加载 `backend/.env` 配置（API Key 等） |

**可选依赖（训练/加载深度学习模型时才用，torch 用 try/except 兜底，不在 requirements.txt）：**

| 库 | 用途 |
|---|---|
| torch / torch.nn / torch.optim / torch.utils.data | 深度学习 DeepNDEClassifier（CNN+BiLSTM+Transformer），训练与推理 |

**系统级外部依赖（非 Python 库，靠系统命令调用）：**

| 依赖 | 用途 |
|---|---|
| antiword | `.doc` 老版 Word 委托单解析（`subprocess` 调用，未安装时返回提示） |
| DeepSeek API | 云端大模型对话（通过 httpx 调用，需 `.env` 配 API Key） |

---

## 二、前端（JavaScript / JSX）

`frontend/package.json` 的 dependencies：

| 库 | 用途 |
|---|---|
| react / react-dom | React 18 框架 |
| antd | UI 组件库（Layout、Upload、Table、Tabs、Alert 等） |
| axios | HTTP 请求后端 API |
| echarts + echarts-for-react | 图表渲染（A-Scan 波形、B-Scan 热力图等） |
| react-markdown + remark-gfm | AI 对话回复的 Markdown 渲染（含表格） |
| react-json-view | JSON 查看/编辑（HDF5 节点 Inspect） |
| react-window | 虚拟滚动（原始帧矩阵 MatrixViewer） |
| react-data-grid | 数据网格表格 |

devDependencies：

| 库 | 用途 |
|---|---|
| vite | 前端构建/开发服务器（`npm run dev`） |
| @vitejs/plugin-react | Vite 的 React 插件 |

---

## 三、C扫集成（规划中，将新增）

| 库 | 用途 | 安装位置 |
|---|---|---|
| ultralytics | YOLOv8 目标检测（C扫缺陷识别） | `ascan_env`，`pip install ultralytics` |
| opencv-python | 图像读取/标注框绘制（备用，实际画框走前端 canvas） | 同上 |

> 注：`ascan_env` 已装 torch 2.0.1+cu118、cv2 4.8.0，仅需补装 ultralytics。
