# 学习计划：Mini_myHDF5_v5 复合材料超声NDE智能检测评估系统

## Context

你希望系统性地学习理解 Mini_myHDF5_v5 这个项目的完整架构和核心逻辑。这是一个**复合材料超声无损检测（NDE）智能评估系统**，已从 v4 的纯数据查看器演进为 v5 的完整智能检测评估平台。该学习计划旨在帮你按"数据流→模块→集成"的顺序高效掌握全部代码。

---

## 第一阶段：全局鸟瞰（30分钟）

### 1.1 项目是什么？
- 读取 `README.md` — 了解项目结构、功能列表、启动方式
- 读取 `会话记录_20260611-0614.md` — 了解最近的开发历程和设计决策
- 读取 `sci.md` — 了解这个项目的学术方向（SCI 论文三个方向）

### 1.2 数据流总览
理解一条完整的数据处理链路：

```
.nde文件上传 → HDF5解析 → 信号特征提取 → ML模型预测 → AI对话评估 → 验收判定 → 报告生成
```

核心概念速查表：

| 概念 | 说明 |
|------|------|
| .nde 文件 | HDF5 格式的超声检测数据文件（64帧 × 2000采样点） |
| A-Scan | 单帧超声波形（1D 信号） |
| B-Scan | 64 帧 A-Scan 组成的 2D 热力图 |
| 缺陷类型 | Cp气孔/Db脱粘/Dl分层/Fb外来物/In夹杂/OK无缺陷/Po孔隙/Rs树脂/Vo空洞 |
| 材料体系 | CF碳纤维/GF玻璃纤维 + EP环氧/BMI双马/PI聚酰亚胺等基体 |
| 命名规范 | `{纤维}_{基体}_{结构}_{方法}_{缺陷}_{型号}_{时间戳}_{材料牌号}.nde` |

---

## 第二阶段：后端核心 — 从数据到文件（2小时）

**目录结构总览**（本阶段地图，共 3928 行 Python 源码）：

```
backend/
├── .env                      # 环境变量（DeepSeek API key 等）
├── requirements.txt          # Python 依赖
├── main.py                   # FastAPI 主入口（1517行）★ 所有 API 端点
├── trainer.py                # 模型训练引擎（1666行）★ 最复杂
├── signal_analysis.py        # 信号特征提取（350行）
├── acceptance_checker.py     # 验收判定规则引擎（306行）
├── deepseek_client.py        # DeepSeek SSE 流式客户端（89行）
├── models/                   # 已训练模型（RF .pkl / DL .pt + 对应 .json 指标）
├── disputes/                 # 争议项存档（DSP-xxx/ 目录 + 证据）
├── reports/                  # 生成的检测报告 .docx
└── __pycache__/              # Python 缓存（忽略）
```

### 2.1 入口与路由 — `backend/main.py`（1470行，最核心）

**学习顺序**（按API端点分组）：

#### A. 文件上传与HDF5解析（~140行）
- `/upload` (L52-72) — 单文件上传，支持 .nde/.h5/.csv/.zip
- `/tree` (L104-124) — 递归构建 HDF5 树形结构 `build_tree()`
- `/dataset` (L126-248) — **核心数据读取**，按数据类型返回：
  - `waveform` — 1D 波形数据（取前4000点）
  - `image` — 2D 图像
  - `nde_tensor` — 3D 张量 [64,1,2000]，展开为 bscan + ascan
  - `json` — JSON 元数据
  - `scalar` — 标量值

#### B. 缺陷标注系统（~120行）
- `/get_frame_labels` (L631-648) — 读取逐帧标签
- `/save_frame_label` (L651-669) — 保存单帧标签
- `/save_frame_labels_batch` (L672-693) — 批量保存
- `/auto_label_frames` (L705-791) — **自动标注算法**：以用户标记的 OK 帧为基准，用相似度公式 `0.5×相关系数 + 0.15×均值差异 + 0.2×能量比 + 0.15×标准差比` 判断其他帧
- `FRAME_LABEL_OPTIONS` (L586-603) — 14种标签（未标注/无缺陷/分层/脱粘/孔隙/气孔/夹杂/纤维相关/富树脂/贫胶/耦合不良/噪音起始/噪音中止/波形过渡/不可分类/信号质量变化）

#### C. 模型训练 API（~40行）
- `/train/preview` → `preview_dataset()` — 数据集统计
- `/train/start` → `start_train()` — 异步启动训练（返回 job_id 轮询）
- `/train/status/{job_id}` / `/train/result/{job_id}` — 进度与结果
- `/train/models` — 列出已训练模型

#### D. 模型测试 API（~30行）
- `/test/start` → `start_test()` — 异步测试
- `/test/file_preview` → `load_file_for_preview()` — 预览单文件 B-scan/A-scan

#### E. AI 对话评估（~300行，最复杂）
- `/chat/ask` (L986-1353) — **SSE 流式对话接口**
  - 构造 system_prompt（材料参数 + 信号特征 + 模型预测 + 验收标准 + 争议项）
  - 支持 cloud（DeepSeek API）/ local（本地模型预测）两种模式
  - 用户质疑时触发争议项流程
  - 检测关键词自动注入数据集/验收标准/争议项上下文
- `/chat/report` → `generate_inspection_report()` — 报告生成
- `/chat/signal_waveform` → `analyze_waveform_per_frame()` + `analyze_waveform_keypoints()`

#### F. 验收判定 API（~70行）
- `/acceptance/standards` — 列出标准
- `/acceptance/check` → `checker.check()` — 执行判定
- `/acceptance/suggest_standard` → `checker.suggest_standard()` — 自动推荐标准

#### G. 争议项 API（~60行）
- `/dispute/submit` — 登记争议（保存 .nde + 证据 ZIP + metadata.json）
- `/dispute/list` — 列出所有争议项

#### H. 委托单 API（~30行）
- `/dispatch/upload` → `parse_dispatch_doc()` — 解析 .doc 委托单

#### I. 数据库总览
- `/dataset_overview` (L1407-1469) — 扫描 dataset/ 目录，按文件名正则解析字段
- `get_dataset_summary()` (L1356-1404) — 统计各类缺陷/材料/结构/方法的分布

### 2.2 模型训练引擎 — `backend/trainer.py`（1640行）

**学习重点**：

1. **特征提取** `extract_features()` (L88-124)
   - 64行 A-Scan × 6种统计量（mean/std/max/min/range/RMS）= 384维
   - 全局6维 = 共 390维信号特征
   - 可选 + 8维元数据 one-hot = 398维

2. **RandomForest 训练** `train_model()` (L240-287)
   - sklearn RandomForestClassifier，支持 n_estimators/max_depth 调参
   - 自动 train_test_split（stratify 分层抽样）
   - 输出 accuracy + confusion_matrix + classification_report

3. **深度学习模型** `DeepNDEClassifier` (L336-426)
   - 架构：InstanceNorm → CNN（逐 A-scan）→ BiLSTM → Transformer → MeanPool → FC
   - 是理解 PyTorch 模型设计的优秀范例

4. **数据集加载** `load_dataset()` (L150-236)
   - 遍历 dataset/ 子目录，用 HDF5 读取数据
   - 优先从 `/Private/GlobalLabel` 读标签，兜底用目录名
   - 支持元数据编码 `encode_metadata()` (L58-86)

5. **后台任务** `_tasks` (L720-829)
   - 线程池异步训练/测试，前端轮询 job_id

6. **报告生成** `generate_inspection_report()` (L1365-1533)
   - 基于 Word 模板（`templates/超声检测报告.docx`）的占位符替换
   - 在 XML 层合并相邻 run 后替换 `{$...}` 占位符
   - 回退方案 `_generate_report_fallback()` — 纯程序化生成

7. **委托单解析** `parse_dispatch_doc()` (L1262-1362)
   - 用 antiword 将 .doc → .txt，解析表格行
   - 中文字段标签映射（如 "委托单位" → "Customer_Addr"）

### 2.3 信号分析 — `backend/signal_analysis.py`（340行）

**3个核心函数**：

1. `analyze_signal()` (L78-232) — 全面信号特征提取
   - 幅值范围/能量/SNR/均值/标准差/峰值位置
   - 底波能量比（backwall_ratio = 后半段/前半段能量）
   - 衰减系数（首行vs末行能量比）
   - 异常区域检测（滑动窗口能量比 > μ+2σ）
   - 验收专用特征：幅值衰减百分比、连续异常帧数、峰值偏移、帧间相关、孔隙率估算

2. `analyze_waveform_per_frame()` (L235-290) — 逐帧能量 + 异常帧检测（IQR方法）

3. `detect_surface_and_backwall()` (L293-327) — 单帧界波/底波识别

### 2.4 验收检查器 — `backend/acceptance_checker.py`（310行）

**核心类 `AcceptanceChecker`**：

- `check()` (L80-206) — 核心判定流程：
  1. 加载标准 → 2. 匹配缺陷类型 → 3. 逐条评估规则 → 4. 判断合格/不合格/无法判定
- `_eval_rule()` (L219-256) — 规则引擎，支持 `and`/`or` 逻辑组合
- `_eval_single_rule()` (L258-291) — 单条件评估（>= / <= / > / < / == / !=）
- 标准数据来自 `standards/` 目录的 `index.json` + `summary.json`

### 2.5 DeepSeek 客户端 — `backend/deepseek_client.py`（90行）

- `chat_stream()` (L17-89) — httpx 异步流式调用 DeepSeek Chat API
- SSE 协议解析：逐行读 `data: {...}` → 提取 `delta.content`
- 支持 deepseek-chat (V3) 和 deepseek-reasoner (R1)

---

## 第三阶段：前端核心 — 用户交互（1.5小时）

**目录结构总览**（本阶段地图，共 4130 行源码）：

```
frontend/
├── index.html                 # 入口 HTML
├── vite.config.js             # Vite 配置（仅 react 插件，无代理/别名）
├── package.json
└── src/
    ├── main.jsx               # React 挂载入口
    ├── App.jsx                # 根组件（704行）★ 状态中心
    ├── components/
    │   ├── EvaluationPanel.jsx  # 智能评估·AI对话（1004行）★ 最复杂
    │   ├── TestingPanel.jsx     # 模型测试（645行）
    │   ├── TrainingPanel.jsx    # 模型训练（483行）
    │   ├── AScanViewer.jsx      # A-Scan波形+逐帧标注（449行）
    │   ├── InspectPanel.jsx     # JSON查看/编辑（220行）
    │   ├── AScanPlayer.jsx      # 全帧波形播放器（200行）
    │   ├── HeatmapViewer.jsx    # 热力图（156行）
    │   ├── MatrixViewer.jsx     # 矩阵表格（138行）
    │   └── DatabaseOverview.jsx # 数据库概览（131行）
    └── 草稿/                   # 旧版 App.jsx 备份（4份，应清理）
```

### 3.1 根组件 — `frontend/src/App.jsx`（705行）

**左侧边栏** — Ant Design Collapse 折叠面板，5个模块：
1. **数据标注** — 上传文件 + HDF5树浏览器 + 批量标签
2. **数据库** — NDE 方法分类导航（超声/射线/光学/电磁）
3. **模型训练** — TrainingPanel
4. **模型测试** — TestingPanel
5. **智能评估** — EvaluationPanel

**右侧内容区** — 用 `display:none` 切换模块（防止组件卸载丢状态）：
- **Inspect** 面板 — JSON 查看/编辑/下载
- **Display** 面板 — Matrix / A-Scan / Heatmap 三 Tab
  - AScanViewer — 波形图 + 帧滑块 + 回放 + 逐帧标注（14种标签+键盘快捷键）
  - HeatmapViewer — B/C-Scan 热力图（支持色图切换、维度翻转、反向）
- 帧标签管理：`frameLabels` 状态 + `handleSaveFrameLabel` 乐观更新

### 3.2 智能评估面板 — `frontend/src/components/EvaluationPanel.jsx`（1005行）

**核心交互流程**：
```
上传 .nde 文件 → 选择模型 → 输入问题 → SSE 流式接收 AI 回复 → Markdown 渲染
                                                              ↓
                                              自动检测是否需争议登记/报告生成
```

**状态管理**：
- `aiMode` — cloud（DeepSeek）/ local（本地模型预测）
- `messages` — 多轮对话历史
- `streamingText` — 实时流式文本
- `signalWaveform` — A-Scan 波形数据（含异常帧标记）
- `showDisputePrompt` / `showReportPrompt` / `showDispatchPrompt` — 条件弹窗

**关键子功能**：
- 争议项登记：用户质疑 → AI 回复提及争议 → 弹出证据上传表单 → POST `/dispute/submit`
- 报告生成：检测"报告"关键词 → 引导上传委托单 → 解析 → 生成报告按钮 → 下载 .docx
- 拖拽上传：支持拖放 .nde/.h5/.hdf5 文件

### 3.3 其他关键组件

| 组件 | 功能 | 亮点 |
|------|------|------|
| `AScanViewer.jsx` | ECharts 波形图 + 帧滑块 + 自动回放 + 14种标签键盘标注 | 信号质量变化检测 |
| `AScanPlayer.jsx` | 全帧波形播放器，异常帧红色标注 | 界波/底波标记 |
| `HeatmapViewer.jsx` | Plotly 热力图，支持色图/维度/反转切换 | 大数据量优化 |
| `TrainingPanel.jsx` | 模型训练配置面板（RF/DL切换 + 超参数 + 进度条） | 元数据特征开关 |
| `TestingPanel.jsx` | 模型测试（选模型 → 运行测试 → 混淆矩阵 + 错误文件列表） | 单文件预览 |
| `InspectPanel.jsx` | JSON 查看器 + 编辑 + 下载 | — |
| `MatrixViewer.jsx` | 矩阵数据表格查看 | — |
| `DatabaseOverview.jsx` | dataset/ 目录浏览 + 文件解析 | — |

---

## 第四阶段：标准与数据（30分钟）

### 4.1 验收标准 — `standards/` 目录
- `index.json` — 可用标准列表（含适用材料）
- `HB/summary.json` — HB 7224-2020 结构化条款
- `GJB/summary.json` — GJB 1038.1A-2004 结构化条款

### 4.2 数据集 — `dataset/` 目录
- 1,902 个 .nde 文件，按缺陷类型分目录（Cp/Db/Dl/Fb/In/OK/Po/Rs/Vo/TODO）
- 文件命名包含完整元数据：`{纤维}_{基体}_{结构}_{方法}_{缺陷}_{型号}_{时间戳}_{材料牌号}.nde`

### 4.3 模板 — `templates/` 目录
- `超声检测报告.docx` — 含 `{$...}` 占位符的 Word 报告模板
- `委托单模板.doc` — 委托单模板

---

## 第五阶段：动手练习（建议）

1. **修改 system_prompt** — 在 `main.py` 的 `/chat/ask` 中调整 AI 助手的行为
2. **添加新的信号特征** — 在 `signal_analysis.py` 的 `analyze_signal()` 中添加新指标
3. **调整自动标注阈值** — 修改 `main.py` 的 `/auto_label_frames` 中的相似度权重
4. **训练自己的模型** — 通过前端 TrainingPanel 或直接调用 `trainer.py`
5. **添加新的验收标准** — 在 `standards/` 下新增标准目录和 summary.json

---

## 学习路径图

```
第1天（2h）：全局鸟瞰 + 后端数据流（main.py 前半 + signal_analysis.py）
第2天（2h）：模型训练引擎（trainer.py）+ 验收检查器（acceptance_checker.py）
第3天（2h）：AI 对话评估（main.py 后半 + deepseek_client.py）+ 前端总览（App.jsx）
第4天（2h）：EvaluationPanel.jsx + 其他前端组件
第5天（1h）：动手练习 + 阅读研究报告/sci.md
```
