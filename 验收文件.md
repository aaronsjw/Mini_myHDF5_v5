# 验收文件功能设计方案

## 一、文件存放位置

在项目根目录下新建 `standards/` 目录，按验收标准分类存放：

```
Mini_myHDF5_v5/
├── backend/
├── frontend/
├── dataset/
├── standards/                    ← 验收文件存放目录
│   ├── README.md                 ← 说明文件清单
│   ├── GJB/                      ← 国军标
│   │   ├── GJBxxxx-2023.pdf
│   │   └── summary.json         ← 结构化摘要
│   ├── HB/                       ← 航空行业标准
│   │   ├── HBxxxx-2022.docx
│   │   └── summary.json
│   └── internal/                 ← 内部验收规范
│       ├── 某型号验收规范.pdf
│       └── summary.json
│   └── index.json                ← 所有验收文件索引
```

## 二、架构设计

```
用户上传 .nde → 信号分析 → 模型预测
                              ↓
                    缺陷检测结果（类型、置信度、异常帧区间…）
                              ↓
                  ┌─── 调用验收标准检查 ───┐
                  │                      │
                  ↓                      ↓
           机器学习规则引擎         LLM 辅助判据
          (结构化规则匹配)       (解读PDF/文档中的条款)
                  │                      │
                  └──────┬───────────────┘
                         ↓
               输出判定结果：
               - 合格 / 不合格
               - 超标项及依据条款
               - 建议补充检测方法
```

## 三、具体实现方案

### 3.1 验收标准结构化格式 (`standards/`)

每份验收文件配一个 `summary.json`，把关键条款结构化：

```json
{
  "standard_id": "HBxxxx-2022",
  "name": "碳纤维复合材料超声检测验收标准",
  "source_file": "HBxxxx-2022.docx",
  "effective_date": "2022-01-01",
  "defect_criteria": [
    {
      "defect_type": "分层",
      "severity_levels": [
        {
          "level": "A",
          "condition": "单帧幅值衰减 > 50%",
          "action": "不合格"
        },
        {
          "level": "B",
          "condition": "连续3帧以上异常",
          "action": "不合格"
        }
      ],
      "needs_cscan": true,
      "cscan_criteria": "C-scan 缺陷面积 > 100mm² 为不合格"
    },
    {
      "defect_type": "孔隙",
      "condition": "孔隙率 > 2%",
      "action": "不合格",
      "needs_cscan": true
    }
  ],
  "complementary_methods": {
    "分层": ["超声CScan", "激光剪切散斑"],
    "脱粘": ["红外热成像IRT", "超声CScan"],
    "孔隙": ["工业CT"]
  }
}
```

### 3.2 后端实现 (`backend/acceptance_checker.py`)

```python
"""
验收标准检查模块
工作流程：
1. 加载 standards/index.json 获取可用的验收标准列表
2. 选择适用的标准（根据材料/结构类型自动匹配或手动选择）
3. 用规则引擎逐条匹配缺陷检测结果
4. 输出判定结果
"""
import json
import os
from typing import Optional

class AcceptanceChecker:
    def __init__(self, standards_dir: str = None):
        self.standards_dir = standards_dir or os.path.join(
            os.path.dirname(__file__), "..", "standards"
        )
        self.standards_index = self._load_index()
        self.loaded_standards = {}

    def list_standards(self):
        """列出所有可用的验收标准"""
        return self.standards_index

    def load_standard(self, standard_id: str):
        """加载指定的验收标准"""
        ...

    def check(self, standard_id: str, detection_result: dict, meta: dict):
        """根据验收标准检查缺陷是否超标"""
        # 1. 逐条匹配缺陷类型
        # 2. 判断是否超标
        # 3. 如果需要C-scan/其他方法但数据不足，给出建议
        ...

    def _match_criteria(self, defect_type: str, criteria: list, signal_data: dict):
        """匹配具体条款"""
        ...
```

### 3.3 API 端点 (`backend/main.py`)

```python
@app.get("/acceptance/standards")
def list_acceptance_standards():
    """列出所有可用的验收标准"""
    checker = AcceptanceChecker()
    return {"standards": checker.list_standards()}

@app.post("/acceptance/check")
def acceptance_check(req: dict):
    """
    验收判定
    输入: {
        "standard_id": "HBxxxx-2022",
        "defect_type": "分层",
        "confidence": 0.85,
        "signal_features": {...},
        "prediction": {...}
    }
    输出: {
        "passed": false,
        "violations": [...],
        "suggestions": [...]
    }
    """
    ...
```

### 3.4 前端集成

在智能评估（Evaluation Panel）对话中，检测到用户要求"验收判定"时，调用 API 并展示结果卡片：

```
┌──────────────────────────────────┐
│  🔍 验收判定结果                  │
│  ─────────────────────────────── │
│  标准：HBxxxx-2022               │
│  零件：CF_EP_Plate_WRUT_Dl_...   │
│  ─────────────────────────────── │
│  ❌ 不合格                       │
│  ─────────────────────────────── │
│  超标项：                        │
│  ① 分层 - 帧 12-15 幅值衰减>50% │
│     → 违反 HBxxxx-2022 第 5.3 条 │
│  ─────────────────────────────── │
│  💡 建议：                       │
│  ① 补充超声CScan确认缺陷面积     │
│  ② 建议激光剪切散斑辅助验证      │
│  ─────────────────────────────── │
│  [查看标准原文] [导出判定报告]     │
└──────────────────────────────────┘
```

## 四、实现步骤（按顺序）

| 步骤 | 内容 | 工作量 |
|------|------|--------|
| 1 | 创建 `standards/` 目录结构 + `index.json` | ⭐ 小 |
| 2 | 把已有的验收文件放入对应的子目录 | ⭐ 小 |
| 3 | 为每份验收文件写 `summary.json`（结构化条款） | ⭐⭐ 中 |
| 4 | 实现 `backend/acceptance_checker.py` 规则引擎 | ⭐⭐ 中 |
| 5 | 添加 API 端点 `/acceptance/standards` 和 `/acceptance/check` | ⭐ 小 |
| 6 | 前端集成——在 Evaluation Panel 添加"验收判定"功能 | ⭐⭐ 中 |
| 7 | 生成判定报告（PDF/DOCX 导出） | ⭐⭐ 中 |

## 五、关键设计决策

### 为什么不直接用 LLM 读 PDF 判定？

因为**验收条款是确定性规则**，必须严格按标准执行。LLM 解读 PDF 可能有歧义，所以需要：
- 结构化 `summary.json` 作为**规则引擎**的依据
- LLM 只用于：辅助理解模糊条款、生成自然语言解释、推荐补充方法

### 对于"需要 C-scan 才知道尺寸"的情况

规则引擎检测到缺陷类型需要尺寸测量时（如分层面积 > 100mm²），自动返回：

```json
{
  "passed": null,
  "reason": "单纯从超声 AScan 无法获得缺陷尺寸信息",
  "suggestions": [
    "建议补充超声 CScan 以测量分层面积",
    "建议红外热成像 IRT 辅助验证"
  ]
}
```
